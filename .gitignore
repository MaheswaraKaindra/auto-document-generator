.env
venv/
__pycache__/
*.pyc
.pytest_cache/
PERAN1_NOTES.md
*.msi

# Dokumen hasil generate saat testing manual + file lock Word
output_*.docx
~$*

# Artefak run validasi (Contract A, docx, laporan) — hasil eksperimen, bukan
# bagian dari repo. Yang di-commit cuma harness dan daftar kasusnya.
scripts/validation/out/

# Dokumen referensi internal (mis. SDD contoh dari perusahaan) — repo ini
# ditujukan jadi produk publik, jangan sampai ikut ter-commit.
*.pdf
