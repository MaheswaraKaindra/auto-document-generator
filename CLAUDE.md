# auto-document-generator

> **Baru bergabung / melanjutkan kerja orang lain?** Baca `SESSION.md` dulu — isinya
> apa yang dikerjakan sesi terakhir dan dari mana sebaiknya melanjutkan. File ini
> (CLAUDE.md) adalah pengetahuan permanen tentang produk; `SESSION.md` adalah foto
> sesaat yang **ditimpa habis tiap sesi baru**. Kalau keduanya bertentangan,
> **CLAUDE.md yang benar**.

## Ringkasan

**auto-document-generator** adalah produk **SaaS generik** yang membaca source code dari repo mana pun (GitHub OAuth/PAT, atau ZIP upload — bukan tool internal satu perusahaan) dan otomatis membuat draf dua dokumen yang biasanya ditulis manual oleh developer/QA: **Solution Design Document (SDD)** dan **User Acceptance Test (UAT) Document**, keduanya dalam format `.docx` siap pakai.

## Latar Belakang & Tujuan

Tim development yang bekerja dengan ritme agile/Scrum sering menunda pembuatan dokumen SDD/UAT (atau menulisnya asal-asalan setelah development selesai) karena menulis dokumen ini manual itu lambat dan membosankan — padahal formatnya selalu sama di setiap project, cuma isinya yang beda. Ide awalnya muncul dari pengalaman magang salah satu anggota tim di sebuah perusahaan (melihat bottleneck ini secara langsung), tapi produk ini sengaja dibangun **generik** supaya siapa pun dengan masalah yang sama bisa pakai — bukan cuma perusahaan tempat asal ide tersebut, dan bukan cuma untuk developer (product owner, QA manual, dan user bisnis juga harus bisa memahami hasilnya).

**Target kualitas output**: setara dokumen SDD/UAT enterprise yang biasa dipakai di industri (approval workflow, tabel requirement, use case per aktor, activity diagram per fitur) — bagian yang memang bisa diturunkan dari kode (fitur, use case, diagram arsitektur, activity diagram, test case) diisi otomatis; bagian administratif yang tidak mungkin diketahui dari kode (nomor RFC, cost estimation, tanda tangan approval, mockup UI) sengaja dibiarkan sebagai placeholder manual di template.

**Acuan konkretnya ada**: satu dokumen Solution Design enterprise sungguhan (87 halaman, PDF di root — **di-gitignore karena dokumen internal klien**, jadi tidak ikut ke repo). `app/templates/sdd_template.md` jelas dimodelkan dari dokumen itu. Dibandingkan bab per bab (2026-07-15), strukturnya **sudah cocok hampir seluruhnya** — Revision History, Persetujuan, Deskripsi Aplikasi, Dev System Type, Demografi, System Requirement, How to Access, Infrastructure & Capacity, Application Architecture, Security, Features Requirement, Flow Proses Bisnis, Use Case, Activity Diagram semuanya ada. **Daftar Gambar** dan **Daftar Tabel** kini **SUDAH ada** (2026-07-16 — lihat Riwayat). Yang tersisa cuma **Mockup Website** dan **Mockup Aplikasi** (mustahil dari kode — kandidat slot upload), jadi **jarak struktur ke dokumen acuan praktis tertutup**. Soal Daftar Gambar/Tabel, dua hal yang layak diingat: (1) dulu dicatat remeh — *"mekanis, tinggal dibangkitkan"* — padahal **di dokumen acuan justru dua bagian itulah yang RUSAK**: seluruh entri Daftar Gambar-nya menyebut aplikasi yang sama sekali lain (sisa copy-paste dari dokumen sebelumnya; nama aplikasi asing itu muncul 5× di seluruh 87 halaman dan kelimanya cuma di Daftar Gambar/Tabel), sementara Daftar Tabel-nya kehilangan nomor 6 & 9 dan menduplikasi 5, 13/14, 15/16. **Dua bagian yang kita belum punya ternyata persis dua bagian yang manusia gagal** — justru *karena* mekanis: membosankan, tidak ada yang memeriksa, lalu membusuk. Di sinilah produk ini **lebih baik daripada acuannya**, bukan sekadar setara — dan itu argumen jualan terkuat yang kita punya (sudah dipakai di `README.md`, dianonimkan karena acuan itu dokumen internal klien). (2) Sengaja **SDD saja**: UAT punya nol gambar dan cuma satu tabel isi, jadi memasangnya di sana menghasilkan dua halaman indeks kosong. **Jadi jarak ke dokumen acuan bukan soal struktur**, melainkan kedalaman (87 halaman vs ~25). Soal placeholder kosong sudah selesai sejak 2026-07-15 — 25 dari 28 penanda `(diisi manual)` kini ditanyakan lewat form (lihat `DocumentMetadata`), sisanya 3 blok tanda tangan/sertifikasi yang memang tidak seharusnya diisi sistem. Catatan penting: panjang bukan ukuran profesional — dokumen acuan panjang sebagian besar karena mockup, timeline proyek, dan tanda tangan. Dokumen 30 halaman yang setiap kalimatnya benar lebih berharga daripada 87 halaman yang separuhnya karangan; justru itu nilai jual produk ini.

## Arah Produk (keputusan pemilik project 2026-07-16, dari masukan penguji)

Tiga keputusan yang mengatur prioritas kerja ke depan:

1. **Frontend dinyatakan cukup.** Sudah dilihat & dinilai pemilik project: "sudah bagus". Artinya: **jangan investasi ke desain frontend lagi sampai memang diperlukan** — bukan karena sempurna, tapi karena bukan bottleneck.
2. **Visi input jangka panjang: "masukkan template dokumen Anda" + link repo / upload ZIP.** Masukan penguji: form metadata 25 field terasa membebani (walau opsional) — mengisinya bikin malas, menghabiskan SDM. Arah akhirnya: pengguna meng-upload template dokumen perusahaannya sendiri, sistem men-generate mengikuti template itu. **Belum dikerjakan** — dan sadari implikasinya: template upload TIDAK menghapus masalah field manual (nomor RFC tetap tidak bisa dikarang dari kode; field itu akan balik jadi placeholder di dokumen), jadi form-nya kelak digantikan, bukan sekadar dibuang.
3. **Urutan kerja menuju visi itu**: (a) dokumen SDD hasil generate harus **sesuai dengan template acuan PREMCO** (PDF 87 halaman di root) — *SELESAI secara struktur, bentuk, dan visual (2026-07-16/17: redesign visual + perbandingan berdampingan 12 pasang halaman + logo header). **Catatan 2026-07-18: "SELESAI" itu distempel untuk template `default`; ketika `premco` dibuat (V1) statusnya diwarisi tanpa diperiksa ulang, padahal template `premco` — yang justru dimaksudkan JADI dokumen Pertamina — masih punya gap: font Aptos (bug, harusnya Calibri), How to Access & Infrastructure belum berbentuk tabel. Ketiganya ditutup 2026-07-18 (lihat Riwayat). Sisa gap `premco` yang belum ditutup butuh Contract B: System Requirement 2-tabel, langkah flow bersarang, list fitur utama di Deskripsi Aplikasi — dan perbandingan berdampingan 16 bab `premco`×PREMCO belum pernah dilakukan (yang 12-pasang itu template `default`).***; (b) baru pindah ke penyempurnaan UAT — *template `premco` UAT SELESAI 2026-07-18: dimodelkan dari dokumen UAT PREMCO asli (`2. UAT_...docx` di root, gitignore), diverifikasi end-to-end pada MyPertamina (grouping test case per-layar jalan). Lihat Riwayat*; (c) baru dukungan template lain/generik — *V0 (studi kelayakan pada docx PREMCO asli) dan V1 (template kedua "premco" + multi-template via `template_id`) SELESAI 2026-07-17; lihat `scripts/validation/V0_TEMPLATE_PREMCO.md` dan Riwayat. Yang tersisa dari visi ini = V2: upload template sembarang + kompilasi dibantu LLM + tinjauan pemetaan oleh user; jalur teknisnya sintesis-reference.docx, bukan swap (swap terbukti menghasilkan paket corrupt). **KELAYAKAN V2 TERBUKTI 2026-07-18** — 5 template multi-sumber diukur, arsitektur diputus (kompilasi upload jadi template terdaftar), dan 3 increment (ukur→`TemplateSpec` / sintesis reference.docx / generate-template→isi→render) terbukti sebagai SATU RANTAI $0 pada template non-PREMCO nyata; lihat `scripts/validation/V2_TEMPLATE_MULTISOURCE.md` & Riwayat. **Sisa = implementasi (konsolidasi ke `app/`+tes, LLM auto-usul peta, UI tinjauan, landscape/orientasi per-section), bukan lagi kelayakan.*** *(increment 1 `TemplateSpec`, increment 2 sintesis reference.docx, increment 3 [generator template Jinja + peta bab heuristik], pendaftaran+storage [kompilasi upload jadi template terdaftar yang dirender `generate_docx` lewat reference tersintesisnya sendiri], DAN endpoint upload [`POST /templates` multipart docx → template_id siap pakai di `/documents/generate`] kini KODE ber-tes [2026-07-18/19] — lihat Riwayat; tersisa: LLM auto-usul peta [berbayar — **DE-RISKED 2026-07-20**: prototipe pada `Template SDD` Dynamics buktikan LLM mengubah dokumen 0→4 binding isi, $0,034; PRODUKSIONISASI SELESAI 2026-07-20 — opt-in `use_llm_mapping` (default heuristik $0), ber-tes, jalur berbayar diverifikasi ke API nyata], UI tinjauan+upload di frontend, orientasi landscape per-section.)* "Sesuai sempurna" di sini artinya **kesetaraan struktur dan bentuk penyajian** — bagian yang mustahil dari kode (mockup, timeline, cost, tanda tangan, URL infra) tetap placeholder; mengarang isinya justru merusak nilai jual produk.

## Alur Sistem (Arsitektur End-to-End)

```
Source Provider (GitHub OAuth / GitHub PAT / ZIP upload)
        |
        v
Repository Normalizer  ->  Workspace (format seragam, titik ini Parser
        |                   tidak tahu/peduli asal source-nya)
        v
narrow_to_product()  ->  buang yang bukan produk, berdasarkan manifest repo
        |                (pyproject.toml / package.json). Gagal-membuka:
        |                tidak ketahuan = simpan semua.
        v
Parser (Tree-sitter, deterministik, TANPA LLM)
        |
        v
ParsedRepoContext.json  ==  Contract A  (Peran 1 -> Peran 2)
        |
        v
LLM (Claude, via Anthropic SDK)  ->  cross-repo mapping (FE fetch <-> BE endpoint)
        |                            + narasi dokumen + script diagram Mermaid
        v
DocumentContent.json  ==  Contract B  (Peran 2 -> Peran 3)
        |
        v
Template Engine (Jinja2, Markdown)  +  PlantUML -> PNG (LOKAL, plantuml.jar)
        |
        v
Pandoc  ->  .docx
        |
        v
Solution Design Document (.docx)  /  UAT Document (.docx)
```

Seluruh rantai di atas jalan **di latar belakang** (sejak 2026-07-16). Dari sisi
klien bentuknya tiga langkah, bukan satu:

```
POST /documents/generate   ->  202 + {job_id}      (~50ms; kerjanya belum jalan)
        |                          |
        |                     BackgroundTasks: rantai di atas (~100-190 detik)
        |                          |
        |                     job_store (SQLite): queued -> running -> done/failed
        v                          v
GET /documents/jobs/{id}   ->  status  ->  GET /documents/jobs/{id}/download
```

Alasannya bukan kerapian: menahan pipeline 191 detik di satu request HTTP membuat
produk ini **mustahil di-deploy** — proxy/load balancer memutus di 30-60 detik
(Heroku 30, nginx & AWS ALB 60, Vercel 10-60), sementara server tetap lanjut
bekerja dan tetap membayar LLM untuk dokumen yang tidak pernah sampai ke siapa
pun. Di localhost tidak ada satu pun batas itu, jadi bug-nya tak terlihat.

Prinsip desain: setiap tahap dipisah dengan **kontrak JSON** yang jelas (Contract A, Contract B), supaya tim bisa kerja paralel dan tiap tahap bisa diganti tanpa merusak tahap lain (mis. ganti sumber kode dari GitHub ke ZIP tidak menyentuh kode parser; ganti LLM provider tidak menyentuh kode ingestion/parser).

## Tim & Pembagian Kerja

Tim 3 orang, tiap orang pegang satu tahap pipeline:

| Peran | Tanggung Jawab | Status |
|---|---|---|
| **Peran 1 — Ingestion & Parsing** | Ambil source code dari sumber manapun (GitHub OAuth/PAT/ZIP), normalisasi, ekstrak struktur kode pakai Tree-sitter | ✅ Selesai, sudah di `develop` |
| **Peran 2 — AI & Prompt Engineering** | Cross-repo mapping, panggil LLM untuk isi konten dokumen + script diagram Mermaid | ✅ Sudah lengkap — lihat catatan migrasi provider di bawah |
| **Peran 3 — Backend & Templating** | Endpoint FastAPI orkestrator, template Jinja2, ekspor ke `.docx` via Pandoc, frontend | ✅ Sudah ada — pipeline penuh (ingest → parse → LLM → template → docx) sudah tersambung end-to-end lewat `POST /documents/generate` |

Semua tahap sudah terhubung. Sisa pekerjaan yang belum dilakukan (bukan bug, memang belum dikerjakan) ada di bagian **Keterbatasan** di bawah.

## Kontrak JSON Antar-Tahap

### Contract A — `ParsedRepoContext.json` (Peran 1 → Peran 2)

Diimplementasikan di `app/services/parser_service.py` (`build_parsed_repo_context`), deterministik, **tanpa panggilan LLM**. Mendukung Python, TS/JS/TSX, dan **Java** (deteksi endpoint: FastAPI/Flask-style Python decorator, Express-style call, method-based TS, **Spring MVC annotation**, **Next.js App Router** `route.ts` named-export dengan path dari struktur folder); bahasa lain jatuh ke entry `"type": "other"` minimal.

```json
{
  "project_name": "...",
  "repositories": [
    {
      "repo_tag": "Backend",
      "files": [
        {
          "file_name": "...",
          "file_path": "...",
          "type": "controller|service|repository|model|ui_component|other",
          "dependencies": ["..."],
          "classes": [
            {"class_name": "...", "methods": [{"method_name": "...", "parameters": ["..."], "return_type": "...", "description": "..."}]}
          ],
          "functions": [
            {"function_name": "...", "parameters": ["..."], "return_type": "...", "description": "..."}
          ],
          "api_endpoints": [
            {"method": "GET|POST|PUT|DELETE", "path": "...", "payload": "..."}
          ]
        }
      ]
    }
  ]
}
```

Catatan: `functions[]` adalah tambahan dari kontrak desain awal (dokumen desain awal cuma punya `classes[]`) — top-level function/standalone function juga diekstrak, bukan cuma method di dalam class.

### Contract B — `DocumentContent.json` (Peran 2 → Peran 3)

Diimplementasikan di `app/services/llm_service.py` (`LLMService.generate_document_content(parsed_repo_context, target_doc_type) -> dict`) via Anthropic Claude (model dibaca dari `LLM_MODEL` di `.env`, default `claude-sonnet-5`) dengan **structured output** (`client.messages.parse(output_format=DocumentContent)` — Pydantic model, bukan parsing JSON manual dari teks).

```json
{
  "document_type": "SDD | UAT",
  "app_description": "...",
  "user_roles": [
    {"role_name": "...", "description": "hak akses & tanggung jawab, non-teknis"}
  ],
  "system_requirements": [
    {"name": "Programming Language | Web Framework | Database | ...", "detail": "..."}
  ],
  "feature_requirements": [
    {"feature_name": "...", "description": "..."}
  ],
  "diagrams": {
    "system_architecture": "@startuml ... @enduml (PlantUML component diagram)",
    "component_integration": "@startuml ... @enduml",
    "business_process_flow": "@startuml ... @enduml (activity, alur bisnis end-to-end)",
    "use_case_diagram": "@startuml ... @enduml (actor + usecase + rectangle)",
    "activity_diagrams": [
      {"activity_name": "...", "description": "...", "actor": "...", "pre_condition": "...",
       "steps": ["langkah 1", "langkah 2"], "diagram_script": "@startuml ... @enduml"}
    ]
  },
  "use_cases": [
    {
      "use_case_id": "UC-01",
      "actor": "...",
      "pre_condition": "...",
      "description": "...",
      "acceptance_criteria": ["...", "..."]
    }
  ],
  "business_flow_description": "...",
  "business_flow_steps": ["tahapan bernomor 1", "tahapan 2", "..."],
  "uat_test_cases": [
    {"test_id": "UAT-01", "role": "...", "module": "layar/fitur yang diuji, mis. 'Halaman Login'", "activity": "...", "steps": "...", "expected_result": "..."}
  ]
}
```

`module` ditambahkan 2026-07-18 (untuk grouping test case per-layar di template `premco` UAT — lihat Riwayat). Wajib (bukan Optional) di schema output LLM supaya grouping selalu punya data; **compiler tetap toleran** kalau field ini tak ada (`_group_test_cases` pakai `.get("module")`), jadi Contract B lama tanpa `module` tetap render (jatuh ke satu tabel). Template `default` mengabaikannya. Test case dengan nilai `module` SAMA dikelompokkan jadi satu tabel; prompt mewajibkan nilai konsisten huruf-demi-huruf + urutan berdampingan.

**Skema di atas berubah besar 2026-07-16** (migrasi PlantUML + pendalaman ke bentuk acuan): semua field diagram kini PlantUML (dulu Mermaid; `mermaid_script` → `diagram_script`), activity diagram membawa metadata acuan (`actor`/`pre_condition`/`steps`), dan `user_roles`/`business_flow_steps`/`business_process_flow` baru; `system_requirements` berubah dari `list[str]` jadi terstruktur. **Contract B lama tidak kompatibel** — file `*_contract_b.json` di `scripts/validation/out/` dari sebelum tanggal itu tidak bisa di-render ulang dengan compiler baru.

**`target_doc_type` memengaruhi alokasi kedalaman konten** (bukan cuma label): kalau `SDD`, prompt memprioritaskan kedalaman di `diagrams`/`feature_requirements`/`use_cases` dan `uat_test_cases` cukup representatif; kalau `UAT`, prompt memprioritaskan `uat_test_cases` yang exhaustive (idealnya satu per endpoint/fitur utama) sementara `diagrams`/`use_cases` cukup ringkas.

Sudah diverifikasi lewat pemanggilan API sungguhan, **termasuk setelah urutan prompt dibalik** (2026-07-15, `target_doc_type` kini muncul sesudah Contract A demi caching): pada `dummy_data`, SDD menghasilkan 5 `uat_test_cases` sementara UAT menghasilkan 9, dengan `feature_requirements`/`use_cases`/`activity_diagrams` yang sama — persis pola yang dijanjikan di atas.

**`app_description`, `feature_requirements[].description`, `use_cases[].description`, dan `business_flow_description` wajib ditulis untuk pembaca non-teknis** (product owner, QA manual, user bisnis) — ini prinsip eksplisit di system prompt `llm_service.py`, konsisten dengan positioning produk sebagai SaaS untuk berbagai kalangan, bukan cuma developer.

### `DocumentMetadata` — isian manusia, BUKAN bagian Contract B

Diimplementasikan di `app/api/schemas_document.py`, masuk lewat `document_metadata` di `GenerateDocumentRequest`, dipakai template sebagai `{{ meta.<field> }}`. Isinya 25 field yang **manusia tahu tapi kode tidak akan pernah tahu**: nomor RFC, No. Solution Design, versi, klasifikasi dokumen, ERP/NON ERP, 7 baris Informasi Demografi, How to Access, Infrastructure & Capacity, 3 remark checklist Security (17 dipakai SDD); Related RFC/Work Order, Change Owner, Prepared/Reviewed By + tanggalnya, Distribution List, **Quality Review Method + Document Version No/Date** (3 terakhir ditambah 2026-07-18 untuk tabel Document Information di template premco UAT) (11 dipakai UAT).

**Sengaja tidak digabung ke `DocumentContent`.** Contract B itu kontrak *output LLM*; ini *input manusia*. Arahnya berlawanan — LLM tidak boleh mengarang nomor RFC, dan pengguna tidak menulis use case. Menggabungnya akan memaksa LLM mengisi field yang bukan urusannya.

Semua field `Optional`. Yang kosong (termasuk string berisi spasi) jatuh balik ke penanda `*(diisi manual)*` lewat `_MetadataDict.__missing__` di `compiler_service.py` — jadi `document_metadata: null` menghasilkan dokumen persis seperti sebelum form ini ada. Menambah field cukup menyentuh **schema + template**; sengaja tidak ada daftar field ketiga di compiler yang bisa basi diam-diam.

**Logo perusahaan BUKAN field `DocumentMetadata`** walau sama-sama isian manusia (sejak 2026-07-17): dia `logo_base64` sendiri di `GenerateDocumentRequest`. Kontrak metadata itu "string yang jatuh ke penanda `(diisi manual)` kalau kosong"; logo itu biner yang jatuh ke "tanpa header" — perilaku kosongnya beda, dan menaruh base64 di `meta.*` akan bocor sebagai teks ke template.

## Struktur Proyek

```
app/
  domain/            # data murni + interface (SourceProvider), tanpa dependency framework
    models.py, ports.py, exceptions.py
  ingestion/         # implementasi SourceProvider
    filters.py               # file mana yang relevan: ekstensi, dir yang di-skip
                             #   (node_modules, venv), dir test/contoh, pola nama file test
    manifest.py              # tanya pyproject.toml/package.json: mana yang produk?
    provider_factory.py      # pilih provider (GitHub / ZIP) berdasarkan SourceType
    providers/github_provider.py, providers/zip_provider.py
  services/
    ingestion_service.py     # orkestrasi ingest -> narrow_to_product -> Workspace (Peran 1)
    parser_service.py        # Tree-sitter structural extraction, Contract A (Peran 1)
    github_oauth_service.py  # OAuth GitHub scaffold (Peran 1)
    llm_service.py           # LLMService, Contract A -> Contract B via Claude (Peran 2)
    job_store.py             # status job + path dokumen (SQLite stdlib) — Peran 3
    compiler_service.py      # Contract B -> render Mermaid -> Jinja2 -> .docx (Peran 3)
    template_spec_service.py # V2: ukur docx template user -> TemplateSpec (JSON), deterministik
    template_generator_service.py  # V2: outline TemplateSpec -> template Jinja (+ peta bab heuristik)
    reference_synthesis_service.py # V2: TemplateSpec -> reference.docx tersintesis (logika
                             #   dipindah dari scripts/; CLI build_reference_docx.py pakai ini)
    template_compiler_service.py   # V2: orkestrator — docx upload -> ukur -> generate ->
                             #   sintesis -> simpan+daftar di data/templates/<id>/
  diagram/                   # LAYER DIAGRAM (redesign 2026-07-20, Option C) — IR
                             #   semantik + renderer per-backend, ala AST+codegen.
                             #   BELUM disambung ke pipeline live (DOCX tak berubah);
                             #   fondasi untuk "LLM emit IR" kelak (lihat Riwayat).
    ir/                      # representasi diagram NETRAL (makna, bukan syntax/layout);
                             #   model GRAF (node/edge/lane). base.py + activity.py,
                             #   usecase.py, architecture.py, component.py
    renderer/                # IR -> source backend (IR tak tahu renderer ada)
      base.py                #   DiagramRenderer (antarmuka abstrak)
      plantuml/              #   backend PERTAMA: IR -> PlantUML struktur-saja (theme
                             #     tetap disuntik compiler_service._normalize_plantuml)
  api/
    routes_ingestion.py      # POST /ingest/github, POST /ingest/zip
    routes_auth.py           # GET /auth/github/login, GET /auth/github/callback
    routes_document.py       # POST /documents/{sdd,uat,generate}, GET /documents/jobs/*
    routes_template.py       # V2: POST /templates (upload docx), GET /templates[/{id}]
    schemas.py, schemas_document.py
  templates/
    sdd_template.md, uat_template.md   # template Jinja2 (Markdown) sebelum dikonversi ke docx
    sdd_premco_template.md      # template gaya PREMCO SDD (kompilasi manual V1) —
                                #   dipilih lewat template_id="premco" di form
    uat_premco_template.md      # template gaya PREMCO UAT (2026-07-18): header
                                #   hijau, grouping per-modul, section landscape
    reference.docx             # kerangka TAMPILAN dokumen: tipografi, gaya tabel,
                               #   kaki halaman bernomor. LOGIKA di services/
                               #   reference_synthesis_service.py; regenerasi lewat
                               #   CLI scripts/build_reference_docx.py — JANGAN diedit
                               #   tangan, hasil editnya akan tertimpa.
                               #   Dikecualikan dari pola `*.docx` di .gitignore.
  core/config.py             # baca .env (ANTHROPIC_API_KEY, GITHUB_TOKEN, GITHUB_CLIENT_*,
                             #   TEMPLATES_STORE_PATH untuk data/templates/ V2)
  main.py                    # entrypoint FastAPI, CORS, daftar router

frontend/                    # React + Vite, form sederhana yang hit POST /documents/generate
  src/App.jsx, src/main.jsx

tests/                       # pytest (191 test) — lihat bagian Testing
dummy_data/                  # fixture JSON — dipakai test otomatis DAN testing manual
scripts/                     # utilitas dev, bukan bagian dari aplikasi
  model_getter.py            # cetak daftar model yang tersedia untuk API key kamu
  validation/                # kerangka validasi ke repo publik nyata — lihat Testing
    README.md                #   cara pakai + kenapa ini bukan pengganti pytest
    repos.json               #   10 kasus uji, tiap kasus menguji satu asumsi produk
    run_validation.py        #   harness 2 tahap (gratis / berbayar)
    out/                     #   hasil run (di-gitignore)
```

Root sengaja dijaga cuma berisi file konfigurasi/dokumen (`.env.example`, `.gitignore`, `CLAUDE.md`, `README.md`, `requirements.txt`) — kode dan aset selalu masuk folder. Kalau menambah script utilitas, taruh di `scripts/`, jangan di root.

## Tech Stack

- **Backend**: Python + FastAPI + Uvicorn
- **Ingestion**: PyGithub (GitHub OAuth/PAT), ZIP upload in-memory (dengan guard anti zip-bomb)
- **Parsing**: `tree-sitter` + `tree-sitter-language-pack` — AST-based, generik multi-bahasa (bukan regex, bukan LLM)
- **LLM**: **Anthropic Claude** via official `anthropic` Python SDK, dipanggil langsung (bukan lewat LangChain). Modelnya **bisa diatur** lewat `LLM_MODEL` di `.env` — default `claude-sonnet-5`. Jangan bingung dengan model yang dipakai Claude Code saat mengerjakan repo ini (diatur lewat `/model`): dua-duanya terpisah total.
- **Templating & Export**: Jinja2 (Markdown) → Pandoc (`pypandoc`) → `.docx`. **Tampilannya** (tipografi, gaya tabel, kaki halaman bernomor) datang dari `app/templates/reference.docx` lewat `--reference-doc`, **bukan** dari template Jinja2 — jadi mengubah rupa dokumen umumnya tidak menyentuh markup-nya. File itu dibangun `scripts/build_reference_docx.py`; edit script-nya, jangan docx-nya. **Tiga pengecualian yang memang tidak bisa dibawa reference.docx** (2026-07-17, alasan lengkap di Riwayat): lebar kolom tabel = rasio dash pada separator row template (dibaca karena `--columns=20`); perataan tengah teks header tabel + caption tabel DI BAWAH tabelnya (konvensi acuan; Pandoc selalu menulisnya di atas) + baris tabel tanda tangan setinggi ≥1 inci dan bloknya diikat utuh se-halaman = post-process `_postprocess_docx` di compiler (Word mengabaikan `pPr` dari table style — diprobe); posisi Daftar Isi/Gambar/Tabel SDD = field code Word yang ditanam langsung di template (Pandoc memaku `--toc` di tempat yang salah).
- **Diagram**: **PlantUML** (sejak 2026-07-16; sebelumnya Mermaid via mermaid.ink) — script digenerate LLM, dirender ke PNG **secara lokal** lewat `plantuml.jar` (butuh **Java 17+**; layout engine `smetana` bawaan jar, jadi tidak butuh Graphviz). Dipilih pemilik project lewat perbandingan berdampingan (`scripts/diagram_comparison.py`): gaya UML-nya (aktor stick-figure, oval use case, activity ber-start/end) persis bahasa visual dokumen acuan enterprise, dan render lokal berarti isi diagram tidak pernah meninggalkan mesin. Gaya visual (`!theme plain` + dpi) disuntik compiler, bukan ditulis LLM — filosofi yang sama dengan `reference.docx`.
- **Frontend**: React + Vite (SPA sederhana, satu form)
- **Testing**: `pytest`

## Environment Variables (`.env`, contoh di `.env.example`)

| Variable | Wajib? | Keterangan |
|---|---|---|
| `ANTHROPIC_API_KEY` | Ya (untuk fitur LLM / Peran 2) | https://console.anthropic.com/settings/keys |
| `LLM_MODEL` | Opsional | Model yang dipakai **aplikasi** untuk menulis dokumen. Kosong = `claude-sonnet-5`. Harga per 1M token (input/output, per 2026-07-15): `claude-sonnet-5` $3/$15 (intro $2/$10 s/d 2026-08-31), `claude-opus-4-8` $5/$25, `claude-haiku-4-5` $1/$5. |
| `DATABASE_PATH` | Opsional | Lokasi SQLite untuk status job + dokumen. Kosong = `data/jobs.db` (di-gitignore, dibuat otomatis). **Sengaja bukan folder temp**: generation itu async, dan seluruh guna DB ini adalah bertahan melewati response — bahkan melewati restart. |
| `PLANTUML_JAR` | Opsional | Path ke `plantuml.jar` untuk render diagram lokal (butuh Java 17+ di PATH). Kosong = `tools/plantuml.jar`. Unduh sekali dari https://github.com/plantuml/plantuml/releases. |
| `GITHUB_TOKEN` | Opsional | PAT untuk akses repo privat lewat endpoint ingest berbasis PAT. Repo publik tetap bisa tanpa token, cuma rate-limited 60 req/jam. |
| `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` / `GITHUB_OAUTH_REDIRECT_URI` | Opsional | Cuma perlu kalau mau flow OAuth GitHub beneran jalan (perlu GitHub OAuth App terdaftar — belum ada saat ini, lihat Keterbatasan). |

## Setup Lokal

```bash
# 1. Buat & aktifkan virtual environment
python -m venv venv
venv\Scripts\activate        # Windows
# atau: source venv/bin/activate   # macOS/Linux

# 2. Install dependency Python
pip install -r requirements.txt

# Punya venv dari SEBELUM migrasi Claude (2026-07-15)? Wajib jalankan ulang
# perintah di atas. venv lama tidak punya `anthropic`, dan gejalanya bukan
# test gagal — pytest langsung mati saat collect dengan
# `ModuleNotFoundError: No module named 'anthropic'`, yang gampang disalahartikan
# sebagai migrasinya yang rusak. Paket lama (langchain*, google-genai) juga masih
# menempel dan sudah tidak dipakai; aman dibuang.

# 3. Pandoc wajib ada di sistem untuk fitur export .docx
#    (kalau belum ada, jalankan sekali):
python -c "import pypandoc; pypandoc.download_pandoc()"

# 3b. Diagram dirender LOKAL oleh PlantUML — butuh Java 17+ di PATH, dan
#     plantuml.jar (unduh sekali dari github.com/plantuml/plantuml/releases,
#     asset plantuml-<versi>.jar, simpan sebagai tools/plantuml.jar)

# 4. Isi .env (copy dari .env.example, isi ANTHROPIC_API_KEY minimal)
cp .env.example .env

# 5. Install dependency frontend
cd frontend
npm install
cd ..
```

## Menjalankan Aplikasi

```bash
# Backend (dari root project)
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
# -> Swagger UI: http://127.0.0.1:8000/docs

# Frontend (terminal terpisah)
cd frontend
npm run dev
# -> http://127.0.0.1:5173
```

URL backend frontend dibaca dari `VITE_API_BASE_URL` (taruh di `frontend/.env.local`), fallback `http://localhost:8000` — sejak audit 2026-07-16; sebelumnya hardcoded.

### Daftar Endpoint

| Method | Path | Fungsi |
|---|---|---|
| `GET` | `/health` | Health check |
| `POST` | `/ingest/github` | Ingest repo dari GitHub (OAuth token atau PAT) -> `Workspace` |
| `POST` | `/ingest/zip` | Ingest repo dari file ZIP yang di-upload -> `Workspace` |
| `GET` | `/auth/github/login` | Mulai flow OAuth GitHub (scaffold, belum bisa dipakai sungguhan — lihat Keterbatasan) |
| `GET` | `/auth/github/callback` | Callback OAuth GitHub |
| `POST` | `/documents/sdd` | Terima `DocumentContent` (Contract B) langsung, render jadi SDD `.docx` — untuk testing template tanpa perlu ingest+LLM. **Tidak menerima `document_metadata`** (body-nya murni Contract B); dokumennya keluar dengan penanda `(diisi manual)` |
| `POST` | `/documents/uat` | Sama seperti di atas, untuk UAT `.docx` |
| `POST` | `/documents/generate` | **Endpoint utama, ASYNC.** Balik **202** + `{job_id, status_url}` dalam ~50ms; pipeline penuh (ingest -> parse -> LLM -> compile) jalan di latar belakang. Satu-satunya yang menerima `document_metadata` (isian form, opsional — lihat `DocumentMetadata`) dan `logo_base64` (logo perusahaan → header tiap halaman; divalidasi SINKRON, file rusak = 422 sebelum job dibuat), plus `template_id` ("default"/"premco", ATAU id hasil upload lewat `POST /templates` — kombinasi tak tersedia = 422 sinkron) |
| `GET` | `/documents/jobs/{job_id}` | Status job: `queued`/`running`/`done`/`failed`. **200 walau job-nya gagal** — kegagalannya ada di payload (`error` + `error_status`), karena pertanyaannya sendiri berhasil dijawab |
| `GET` | `/documents/jobs/{job_id}/download` | Unduh `.docx` hasil. **409** kalau job belum selesai (bukan 404 — job-nya ada, cuma belum siap) |
| `POST` | `/templates` | **V2 — upload `.docx` template user.** Multipart (`file`, opsional `name`/`doc_types`/`use_llm_mapping`). **SINKRON**: ukur → peta bab (heuristik $0 DEFAULT; `use_llm_mapping=true` = peta LLM BERBAYAR, satu panggilan Claude per doc_type) → generate template Jinja + sintesis reference.docx → simpan+daftar di `data/templates/<id>/`. Balik **201** + `{manifest, mappings}` (rencana peta bab untuk ditinjau). `.doc`/non-docx/rusak = 422; kebesaran = 413. `template_id` hasilnya langsung bisa dipakai di `/documents/generate` |
| `GET` | `/templates` | Semua template (built-in + hasil-upload) dengan `doc_types`/`source`/`name` — untuk mengisi dropdown gaya dokumen di frontend (menghormati ketersediaan per jenis dokumen) |
| `GET` | `/templates/{id}` | Detail template terkompilasi: manifest + rencana peta bab (untuk UI tinjauan). **404** kalau bukan template hasil-upload |

## Testing

```bash
pytest
```

288 test, **selalu mock** pemanggilan LLM (Claude — termasuk pemeta bab LLM di boundary `_request_bindings`), proses plantuml.jar, dan GitHub — supaya test tidak bergantung pada koneksi internet, Java/jar terpasang, API key, atau kuota, dan tidak pernah mengeluarkan biaya API secara tidak sengaja. (Pengecualian sadar: test V2 template — `test_build_reference_docx`, `test_template_compiler_service`, `test_routes_template` — memakai pandoc ASLI untuk mensintesis/merender reference.docx; itu deterministik & $0, tak keluar ke jaringan.)

| File | Meng-cover |
|---|---|
| `tests/test_compiler_service.py` | Render PlantUML, Jinja2, export docx, metadata dokumen, template premco (bar biru SDD; header hijau + grouping per-modul + section landscape UAT) (Peran 3) |
| `tests/test_routes_document.py` | Endpoint `/documents/*` (Peran 3) |
| `tests/test_routes_ingestion.py` | Endpoint `/ingest/zip`: multipart, pemasangan file↔tag, error 422 (Peran 1) |
| `tests/test_github_provider.py` | Ingest lewat tarball, pakai tarball sintetis di memori (Peran 1) |
| `tests/test_filters.py` | File mana yang relevan; termasuk penjaga anti-rakus (Peran 1) |
| `tests/test_manifest.py` | Deteksi produk, pakai manifest asli fastapi/flask/requests/express/medusa/cal.com (Peran 1) |
| `tests/test_llm_service.py` | Guard context window & deteksi dokumen terpotong; tolak sebelum membayar (Peran 2) |
| `tests/test_parser_endpoints.py` | Deteksi endpoint Python: Flask (`@bp.route`, `url_prefix` Blueprint) & FastAPI (Peran 1) |
| `tests/test_parser_java.py` | Parser Java: endpoint Spring MVC (termasuk prefix class), interface, Javadoc, import (Peran 1) |
| `tests/test_parser_app_router.py` | Deteksi endpoint Next.js App Router: `route.ts` named export + path dari struktur folder (Peran 1) |

**`parser_service.py` ter-cover di bagian deteksi endpoint Python (18 test), seluruh jalur Java (19 test), dan endpoint Next.js App Router (16 test)** — bagian yang paling baru diubah dan paling mahal kalau salah. Ekstraksi class/function Python/TS, heuristik `type`, dan deteksi endpoint Express-style masih tanpa test khusus — lihat Keterbatasan.

### Validasi ke repo publik (`scripts/validation/`)

Ini **bukan pengganti `pytest`** dan menjawab pertanyaan yang berbeda:

| | `pytest` | `scripts/validation/` |
|---|---|---|
| Menjawab | "kode sesuai rancangan?" | "rancangan bertahan di repo asing?" |
| LLM & Mermaid | di-mock | sungguhan |
| Biaya | gratis | GitHub API + (opsional) Claude berbayar |

Buktinya keduanya perlu: bug `414` mermaid.ink lolos dari 11 test yang semuanya hijau, justru karena mermaid.ink di-mock.

```bash
python scripts/validation/run_validation.py --list        # daftar kasus
python scripts/validation/run_validation.py               # Tahap 1: ingest+parse, GRATIS
python scripts/validation/run_validation.py --only flask --with-llm   # Tahap 2: BERBAYAR
```

**Tahap 1 gratis** (tanpa LLM) sudah cukup menjawab "parser paham repo ini?" — Contract A-nya disimpan ke `out/` supaya Tahap 2 tidak perlu ingest ulang. **Tahap 2 berbayar**: satu kasus = satu panggilan Claude, wajib opt-in. Baca `scripts/validation/README.md` sebelum menjalankan yang berbayar.

Untuk testing manual end-to-end (hit API sungguhan, termasuk panggilan LLM yang sesungguhnya) — **lakukan ini hanya saat memang sedang sengaja menguji**, jangan jadikan kebiasaan default karena memakai kuota API berbayar:
- `dummy_data/document_content_sdd.json` / `document_content_uat.json` — contoh `DocumentContent` (Contract B) siap pakai untuk `POST /documents/sdd` / `/documents/uat` langsung (tanpa LLM).
- `dummy_data/parsed_repo_context_sample.json` — contoh `ParsedRepoContext` (Contract A) sintetis (aplikasi task-manager kecil, 2 repo: Backend + FE-Web) untuk diumpankan ke `LLMService.generate_document_content(...)` secara manual, kalau ingin menguji kualitas output LLM tanpa perlu ingest dari GitHub sungguhan.

## Keterbatasan yang Diketahui

- **Endpoint Django belum terdeteksi** (Flask **sudah** sejak 2026-07-16, lihat Riwayat). Django menaruh route di `urls.py` lewat `path()` — bukan decorator sama sekali, jadi `_python_endpoints_from_decorator` tidak bisa melihatnya; perlu jalur pengenalan tersendiri. Terukur: **saleor** 0 endpoint dari 2.573 file. **Sengaja belum dikerjakan**, dan alasannya penting: satu-satunya kasus Django yang kita punya (saleor) tetap terhalang guard context window (1,2 juta token), jadi perbaikannya **tidak bisa dibuktikan end-to-end** — kita cuma akan mengirim kode yang tidak terverifikasi. Butuh aplikasi Django yang lebih kecil di `repos.json` dulu.
- **Next.js App Router SUDAH terdeteksi (2026-07-18); yang tersisa: Nuxt `server/api` + Next.js Pages Router.** App Router (`app/api/**/route.ts`, named export `GET/POST/...`) kini didukung — `_ts_app_router_endpoints` + `_app_router_path`, terukur taxonomy 0→8 endpoint (lihat Riwayat). **Belum**: (1) **Nuxt server routes** `server/api/*.ts` lewat `defineEventHandler` (method dari akhiran nama file: `users.get.ts`→GET, atau `[id].ts`→semua method); (2) **Next.js Pages Router** `pages/api/**` lewat `export default function handler(req,res)` — SATU handler untuk SEMUA method (method dicek runtime via `req.method`), jadi method-nya **tak bisa ditentukan statis** (taxonomy punya satu: `pages/api/auth/[...nextauth].ts`). Keduanya lebih jarang dari App Router di repo baru. **Dampak ketiadaan ini RINGAN & terbukti**: taxonomy sebelum fix (0 endpoint) tetap menghasilkan dokumen berjejak dengan diagram integrasi benar (LLM menyimpulkan dari nama file/fungsi) — jadi ini penurun kualitas potensial, bukan blocker.
- **`url_prefix` Blueprint Flask lewat `register_blueprint()` belum ditangani** (bentuk konstruktor **sudah**, sejak 2026-07-16 — lihat Riwayat). Flask punya dua tempat menaruh prefix, dan yang didukung baru satu: `Blueprint("admin", __name__, url_prefix="/admin")` (di file yang sama dengan route-nya) jalan; `app.register_blueprint(admin_bp, url_prefix="/admin")` (di file lain) **tidak** — path-nya keluar tanpa prefix, seperti perilaku lama. Kalau keduanya ada, Flask memenangkan yang di `register_blueprint`, jadi kita bisa salah di kasus itu. **Sengaja belum dikerjakan, alasannya sama dengan Django**: satu-satunya aplikasi Flask nyata yang kita punya (esteler) memakai bentuk konstruktor, jadi jalur lintas-file **tidak bisa dibuktikan ke repo asli** — kita cuma akan mengirim kode yang tidak terverifikasi, dan menebak prefix yang salah menaruh endpoint di URL yang tidak ada (lebih buruk daripada path kurang lengkap). Menyelesaikannya butuh resolusi impor lintas-file (`from routes.admin import admin_bp as x` → cocokkan `x` balik ke file & variabel aslinya). Butuh aplikasi Flask bergaya itu di `repos.json` dulu.
- **Parser paham Python, TS/JS/TSX, dan Java** (Java sejak 2026-07-16) — bahasa lain perlu ditambahkan kalau dibutuhkan (fallback saat ini: entry `"type": "other"` minimal, tanpa detail class/function/endpoint). Terukur pada **gin-go: 59 file, 0 class, 0 endpoint, Contract A 7,9 KB** — itu ukuran lubang Go, dan bentuknya sama persis dengan Java sebelum didukung. **TAPI — guardrail prioritas (temuan A/B 2026-07-18, jangan diabaikan diam-diam): menambah bahasa/presisi parser kemungkinan besar MARGINAL untuk kualitas dokumen.** A/B taxonomy membuktikan LLM sudah pandai menyimpulkan struktur dari nama file — endpoint eksplisit dari parser cuma menggeser dokumen sedikit (lihat Riwayat). Nilai jual produk = KUALITAS DOKUMEN, dan parser breadth sudah lewat titik hasil-menurun untuk itu. Menambah Go/Kotlin/dst. itu kerja NYAMAN (centang hijau, bisa dari repo saja), bukan kerja BERHARGA — kerjakan hanya kalau ada repo target NYATA yang membutuhkannya, bukan demi kelengkapan. Pekerjaan bernilai-tinggi berikutnya justru terhalang input eksternal (dokumen UAT & template sumber lain) atau ada di tepi deployment (job reaper, jalur ZIP, OAuth), bukan di parser.
- **Repo Java enterprise besar belum pernah diuji — dukungan Java baru terbukti di satu sample app 33 file.** `spring-petclinic` menghasilkan 17 endpoint yang semuanya benar **dan dokumen yang seluruh isinya berjejak** (lihat Riwayat), tapi dia aplikasi contoh, bukan aplikasi bisnis sungguhan. Dua hal yang belum terjawab: (1) **ukuran** — densitas Java terukur **0,64 KB/file**, sebanding saleor (0,81 KB/file, muat di 85%), jadi ekstrapolasi ke ~2.500 file memberi ~677K token (68%, muat); tapi petclinic nyaris tanpa Javadoc, dan repo enterprise ber-Javadoc padat bisa naik ke ~2 KB/file (seperti fastapi) yang berarti ~2,1 juta token — **tidak muat**. Ini ekstrapolasi, bukan bukti. (2) **`@RequestMapping` tanpa `method=`** diaproksimasi GET (lihat komentar di `_java_endpoints_from_annotations`); petclinic memakai `@GetMapping`/`@PostMapping` eksplisit di semua handler-nya, jadi cabang itu belum tersentuh repo nyata — butuh repo Spring bergaya lama. Keduanya butuh kasus uji Java yang lebih besar di `repos.json`.
- **Monorepo besar tetap tidak muat; menyaring TIDAK akan menolong** (diukur 2026-07-16 dalam token, bukan KB). medusa sesudah `workspaces` + compact: **1.253.878 token = 125% context window** → tetap ditolak `_guard_context_window`. Yang tersisa **memang kode produk**, jadi tidak ada lagi yang bisa dibuang tanpa membuang barang asli. Diukur per-field: `api_endpoints` cuma **0,1%** (membuangnya sia-sia), sementara yang mahal justru `classes` (21%) dan `dependencies` (23%) — dua-duanya bahan baku dokumen. Bahkan membuang `classes`+`functions` sekaligus cuma turun ke **97%** — terlalu mepet, bukan solusi. **Masalahnya arsitektur: produk ini mengirim SELURUH Contract A dalam SATU panggilan.** Arah yang mungkin (belum diputuskan): pecah per-workspace-package lalu gabungkan; ringkas dulu pakai model murah; atau kirim struktur tanpa detail. **Catatan urutan prioritas — klaim lama di sini sudah GUGUR.** Dulu tertulis *"memperbaiki dukungan Java/.NET tidak ada gunanya sebelum ini beres, repo enterprise justru yang paling besar"*. Itu ditulis waktu saleor juga tidak muat; sesudah compact, saleor MUAT (85%), jadi yang tersisa tidak muat cuma **monorepo raksasa** — kasus yang jauh lebih sempit dari "repo enterprise". Java dikerjakan 2026-07-16 tanpa menunggu chunking, dan densitasnya terukur **0,64 KB/file** — lebih ringan dari saleor, bukan lebih berat. Pelajarannya: klaim itu adalah **tebakan tentang ukuran yang tidak pernah diukur**, dan dia menahan pekerjaan yang ternyata tidak terhalang.
- **Diagram `system_architecture` menambahkan komponen yang tidak punya jejak di kode** (ditemukan 2026-07-15 lewat Tahap 2, **belum diperbaiki**). Contract A fastapi punya **nol** dependency database (tidak ada sqlalchemy/sqlmodel/psycopg2 — sudah dicek), tapi diagramnya tetap menggambar `Backend --> Database`. FastAPI itu framework yang sengaja tidak punya lapisan database; kotak itu muncul dari kekosongan. Kelas kesalahannya berbeda dari kasus "Hero": di sana LLM membaca kode nyata tapi kode yang salah (tutorial); di sini tidak ada kode apa pun yang mendasarinya. **Penting — jangan salah baca ini sebagai "diagramnya cuma template".** Dugaan awal itu terbantah oleh realworld pada hari yang sama: di sana diagramnya menulis `Database (PostgreSQL via Prisma)`, dan `prisma` memang benar-benar ada 4x di `dependencies`-nya. Jadi LLM **memang** menurunkan diagram dari kode; masalahnya cuma dia tidak berhenti ketika buktinya tidak ada. Dampaknya ringan untuk mayoritas repo target (aplikasi bisnis biasa memang punya database, jadi tebakannya kebetulan benar) — yang perlu diperbaiki: larang eksplisit menggambar komponen yang tidak punya jejak di `dependencies`. **Batasnya makin sempit, bukan makin lebar — fastapi masih SATU-SATUNYA kasus karangan yang terkonfirmasi.** Pada esteler-app (2026-07-16) diagramnya menulis `Groq`, `Cloudinary`, dan `Database PostgreSQL (Neon)`, dan **semuanya punya jejak**: `Groq` ×1 dan `cloudinary`/`cloudinary.uploader`/`CloudinaryError` dari `dependencies`, `SQLAlchemy` ×1 untuk Database, dan **"Neon" + "PostgreSQL" dari DOCSTRING** `config.py::_normalize_db_url()` ("Normalisasi DATABASE_URL agar selalu siap pakai untuk Neon. — postgres:// → postgresql:// ..."). **Catatan untuk siapa pun yang mengaudit karangan di kemudian hari: `dependencies` BUKAN satu-satunya bukti di Contract A.** `functions[].description` dan `classes[].methods[].description` membawa docstring, dan LLM memang membacanya — sesi ini sempat salah menuduh "Neon" sebagai karangan justru karena cuma menggeledah `dependencies`. Geledah seluruh Contract A (`json.dumps(ctx)`), bukan satu field. Yang perlu diperbaiki tetap sama: larang eksplisit menggambar komponen yang tidak punya jejak **di mana pun** di Contract A — **larangan itu SUDAH ditulis ke SYSTEM_PROMPT 2026-07-16** ("ATURAN BUKTI UNTUK DIAGRAM", ikut migrasi PlantUML), tapi efeknya **belum diverifikasi** ke kasus fastapi; kasus itu tetap terbuka sampai ada regen fastapi yang diagram-nya berhenti mengarang Database. **Diperkuat lagi oleh petclinic (2026-07-16, Java)**: diagramnya menggambar `Backend --> Database`, dan jejaknya kuat (`JpaRepository`, `Entity`×18, `Table`, `Column`, `ManyToOne`, `Transactional`) — begitu pula "Server-rendered Views" (`ModelAndView`, `WebMvcConfigurer`) dan "pergantian bahasa" (`LocaleResolver`, `LocaleChangeInterceptor`). Jadi **tiga repo berturut-turut** (realworld, esteler, petclinic) menggambar dari bukti, dan fastapi tetap SATU-SATUNYA karangan yang terkonfirmasi. Ini menaikkan dugaan bahwa pemicunya spesifik: fastapi itu *framework* yang sengaja tidak punya lapisan database, jadi kotak Database muncul justru dari **kekosongan** — bukan dari kebiasaan menggambar template.
- **Kualitas `claude-sonnet-5` vs `claude-opus-4-8` untuk tugas ini BELUM diuji.** Default dipindah ke Sonnet 5 pada 2026-07-15 atas keputusan pemilik project, alasannya biaya (Opus ~$1,36/dokumen untuk repo sebesar fastapi; tim menganggap $0,50 sudah mahal). Dasarnya reputasi umum Sonnet 5 yang mendekati Opus, **bukan** perbandingan langsung pada repo nyata — jadi ini asumsi, bukan temuan. Menguji ini murah dan berdampak lama: satu repo × dua model (~$1 sekali bayar) lalu **baca kedua dokumennya**. Kalau kualitasnya turun, ganti balik lewat `LLM_MODEL=claude-opus-4-8` di `.env` (tanpa sentuh kode). Ingat produk ini menjual kualitas dokumen — itu satu-satunya nilainya, jadi jangan biarkan asumsi ini menggantung terlalu lama.
- **Kontaminasi input jauh lebih berbahaya daripada label yang salah** (ditemukan 2026-07-15 lewat Tahap 2; **sudah diperbaiki** lewat deteksi manifest — lihat Riwayat Perubahan. Dicatat di sini karena pelajarannya masih berlaku untuk perubahan berikutnya). Terbukti lewat perbandingan langsung: **flask** (coverage 0%, semua file `"other"`) menghasilkan SDD yang **akurat** — "Flask adalah kerangka kerja pengembangan aplikasi web" dengan fitur Routing/Request-Response/Session/Template yang semuanya benar. Sebaliknya **fastapi** (coverage 63%) menghasilkan SDD yang **percaya diri tapi salah**: "Sistem ini adalah kumpulan aplikasi backend... mengelola data, otentikasi, unggah berkas", dengan fitur "Manajemen Item" dan "Manajemen Hero". FastAPI itu *framework*, dan "Hero" itu contoh dari tutorial SQLModel. Sebabnya: **456 dari 530 file (86%) berasal dari `docs_src/`** — folder tutorial dokumentasi — dan **seluruh 432 endpoint** berasal dari sana, sementara package `fastapi/` yang asli menyumbang **0 endpoint**. Ini kelas bug yang sama dengan `test/` pada express, tapi lewat pintu berbeda: `docs_src` tidak ada di blocklist. Menambahkan `docs_src` cuma menunda masalah — tiap repo mengarang konvensinya sendiri (`test/`, `examples/`, `docs_src/`, `website/`, `playground/`), jadi daftar nama tidak akan pernah lengkap. Arah yang lebih menjanjikan: **tanya repo-nya sendiri apa yang dia kirim** — `pyproject.toml`/`package.json` mendeklarasikan nama package-nya (fastapi menyatakan package-nya `fastapi`), jadi prioritaskan file di bawah situ alih-alih menebak-nebak folder mana yang bukan produk.
- **Heuristik `type` menebak dari nama file, dan gagal pada penamaan idiomatik Python/Go** (ditemukan 2026-07-15 lewat validasi, belum diperbaiki — tapi **prioritasnya rendah**, lihat butir di atas: flask dengan coverage 0% tetap menghasilkan dokumen akurat, jadi label ternyata bukan bottleneck-nya. LLM sanggup menyimpulkan peran file dari nama class, nama function, dan `dependencies` yang tetap dikirim di Contract A). `_guess_file_type()` di `parser_service.py:144` mencocokkan substring nama file (`"controller"`, `"service"`, `"model"`, ...) plus satu sinyal isi (`has_endpoints`). Konsekuensinya diukur pada repo **flask**: coverage **0%** — 25 file semuanya `"other"` — padahal parser sukses menarik 52 class dan 72 function dari repo yang sama. Jadi parsing-nya jalan, **pelabelannya** yang gagal: flask menamai file secara idiomatik (`app.py`, `helpers.py`, `wrappers.py`), bukan bergaya MVC Java/Spring (`UserController.java`). Artinya `coverage` tinggi selama ini menandakan "repo ini kebetulan pakai konvensi penamaan Java", bukan "parser paham repo ini". Diperparah karena yang dibaca cuma `file_name`, bukan path — file di `app/controllers/user.py` tetap `"other"` sebab nama filenya cuma `user.py`. Perbaikan yang mungkin (termurah dulu): (1) ikut membaca path, bukan cuma nama file; (2) tambah sinyal berbasis isi (ada import ORM? ada query SQL? ada render komponen?) seperti `has_endpoints` yang sudah terbukti jalan.
- **Validasi menyentuh 10 repo, tapi masih cuma 3 yang pernah benar-benar jadi dokumen** (flask, fastapi, realworld). Ditambah 2026-07-15: `saleor-django` (Django e-commerce), `medusa-monorepo` (monorepo TS), `esteler-flask` (aplikasi Flask milik anggota tim — satu-satunya kode yang tidak ditulis untuk dipamerkan). Tahap 1 jalan ke semuanya; **Tahap 2 ke aplikasi bisnis masih belum pernah berhasil** — saleor ditolak guard context window (1,2 juta token), medusa juga akan ditolak (Contract A 3,4 MB). **Butir ini sebagian besar sudah terlampaui** — hitungannya kini **7**: ditambah `esteler-flask` (aplikasi bisnis Python nyata pertama) dan `spring-petclinic` (Java, keduanya 2026-07-16), lalu **`MyPertamina.id-Clone` (Vue/JS/TS)** dan **`shadcn-ui/taxonomy` (Next.js/TSX, keduanya 2026-07-18 — lihat Riwayat)**. Pertanyaan lama *"kalau coverage rendah, apakah LLM mengarang dari nama file?"* **terjawab TIDAK untuk Java DAN TS/JS**: petclinic coverage 33% & MyPertamina 27%, dokumennya tetap berjejak seluruhnya; taxonomy malah coverage 77% tapi 0 endpoint (App Router tak terdeteksi) — dan dokumennya TETAP berjejak, menegaskan LLM menyimpulkan API dari nama file/fungsi. Yang tersisa: **saleor** (Django) sekarang MUAT (85%) tapi belum pernah dijalankan Tahap 2 — dan 0 endpoint-nya membuatnya kasus paling tajam yang tersedia untuk menguji karangan; **medusa** masih tidak muat; **gin-go** (Go) & repo Java **besar** belum tersentuh.
- **Jalur ZIP → dokumen TIDAK PERNAH tersambung** (ketahuan lewat audit menyeluruh 2026-07-16 — selama ini tidak tercatat di mana pun). Ringkasan produk sejak hari pertama menyebut *"GitHub OAuth/PAT, atau ZIP upload"* sebagai sumber, dan diagram alurnya pun menggambarnya. Kenyataannya: `POST /ingest/zip` berhenti di **Contract A** (analisis kode), sementara `POST /documents/generate` — satu-satunya jalur ke dokumen — cuma menerima `repositories` GitHub (`GenerateDocumentRequest` tidak punya field ZIP sama sekali). Jadi ZIP bisa *dianalisis* tapi tidak bisa *jadi dokumen* tanpa merangkai manual `/ingest/zip` → `LLMService` → `/documents/sdd`. Kelas temuan yang sama dengan Revision History: **klaim ditulis dari desain, bukan dari memeriksa jalurnya ada.** Menyambungkannya butuh `/documents/generate` menerima multipart (bukan JSON murni) atau endpoint generate-ZIP terpisah — keputusan API, belum dikerjakan. README sudah dijujurkan.
- **OAuth GitHub baru scaffold** — endpoint-nya ada tapi belum bisa dipakai sungguhan sampai ada GitHub OAuth App terdaftar (`GITHUB_CLIENT_ID`/`SECRET` belum diisi).
- **`parser_service.py` baru ter-cover sebagian** (Peran 1). `tests/test_parser_endpoints.py` (18 test) mengunci deteksi endpoint Python, `tests/test_parser_java.py` (19 test) mengunci seluruh jalur Java, dan `tests/test_parser_app_router.py` (16 test, 2026-07-18) mengunci endpoint Next.js App Router — bagian yang paling baru diubah dan paling mahal kalau salah. Yang masih kosong: ekstraksi class/function Python & TS, heuristik `type`, dan deteksi endpoint Express-style (`_ts_endpoint_from_call`). `ingestion_service.py` juga masih tanpa test; ingestion sendiri sudah sebagian ter-cover lewat `tests/test_github_provider.py` (jalur tarball, GitHub di-mock lewat tarball sintetis di memori).
- **Cross-repo dependency resolution (FE fetch call <-> BE endpoint) sepenuhnya didelegasikan ke instruksi prompt LLM**, bukan langkah pencocokan deterministik terpisah seperti di diagram arsitektur target (`Cross Repository Mapping` -> `Unified Context` sebelum masuk LLM). **Terbukti jalan** pada `realworld-fullstack` (2026-07-15 — lihat Riwayat Perubahan), jadi cukup untuk MVP. Tapi karena bersandar pada LLM, hasilnya **tidak dijamin deterministik**: belum diuji pada repo dengan pola pemanggilan API yang tidak lazim (mis. URL dirakit dinamis, atau lewat wrapper client berlapis), dan belum pernah dijalankan dua kali pada repo yang sama untuk melihat apakah pemetaannya konsisten.
- ~~**Render diagram lewat layanan hosted (`mermaid.ink`) + batas URL ~8KB-nya**~~ — **SELESAI 2026-07-16** lewat migrasi ke PlantUML lokal (lihat Riwayat): isi diagram tidak lagi meninggalkan mesin, batas panjang URL tidak ada lagi (input lewat stdin), dan larangan paralelisasi 503 mermaid.ink tidak berlaku (render lokal — belum diparalelkan, tapi kini boleh kalau dibutuhkan). Harga yang dibayar: dependency runtime baru — **Java 17+ dan plantuml.jar wajib ada di mesin** (gagal dengan `DiagramRenderError` yang menyebut cara pasangnya kalau tidak ada).
- **`LLMService` memakai timeout client 1500 detik (25 menit)** untuk mengantisipasi generation yang lama pada repo besar/kompleks (default SDK 10 menit terbukti kurang untuk repo nyata yang cukup besar saat diuji). Kalau generation tetap sering lambat di masa depan, pertimbangkan pindah ke `.stream()` daripada menaikkan timeout terus-menerus.
- ~~**Tabel Revision History keluar sebagai baris kosong**~~ — **BUKAN CACAT. Butir ini SALAH, dicoret 2026-07-16.** Tercatat sejak 2026-07-15 sebagai *"dua tabel kosong nongkrong di halaman pertama SDD"* yang katanya butuh keputusan produk. Begitu dokumen acuan benar-benar **dibaca** (87 halaman, PDF di root, ada sejak hari pertama), halaman 2-nya ternyata: `DOCUMENT REVISION HISTORY` + header lima kolom, lalu **satu baris kosong**; `APPLICATION REVISION HISTORY` sama persis. **Output kita sudah identik dengan acuan.** Dokumen Solution Design yang baru dibuat memang belum punya riwayat revisi — tabel itu kolom isian manual untuk revisi *berikutnya*, dan mengisinya justru berarti sistem mengarang riwayat. Kekhawatiran lama (*"`Summary of Changes` tidak punya jawaban jujur untuk dokumen baru"*) ternyata **benar**, dan jawabannya: memang tidak diisi — persis yang sudah dilakukan. **Pelajarannya lebih berharga daripada butirnya**: ini dicap cacat dengan membandingkan output ke *gagasan tentang dokumen yang baik*, bukan ke dokumen acuan yang sudah ada di root repo ini sejak awal. Pola "memeriksa proksi, bukan barangnya" lagi — dan proksinya kali ini **selera**. Sebelum menyebut sesuatu cacat, buka PDF-nya.
- **Job yang sedang jalan hilang kalau prosesnya mati** (batas yang diketahui & diterima sejak async masuk, 2026-07-16). `BackgroundTasks` menjalankan job **di dalam proses yang menerima POST**, jadi kalau uvicorn di-restart (deploy, crash, OOM) saat job jalan, job itu **berhenti selamanya di status `running`** — tidak ada yang memungutnya kembali, dan kliennya akan polling tanpa akhir. Belum ada reaper untuk job `running` yang basi. **Ini keputusan sadar, bukan kelalaian**: penghalang yang sebenarnya adalah request digantung 191 detik lalu diputus proxy, dan itu **sudah selesai** dengan nol infrastruktur baru. Redis + RQ menyelesaikan sisanya tapi menambah layanan yang harus hidup saat deploy. **Multi-worker TIDAK termasuk masalah — sudah diukur, dan jalan** (lihat butir berikut). Catatan penting untuk deploy: **di platform serverless (Vercel/Lambda) `BackgroundTasks` tidak aman** — proses bisa dibekukan begitu response terkirim, jadi job-nya mati di tengah jalan. Butuh worker sungguhan di sana. (esteler-app milik anggota tim ada di Vercel — kalau generator ini ikut ke sana, ini penghalangnya.)
- **Multi-worker JALAN — jangan percaya klaim sebaliknya** (diukur 2026-07-16 di server hidup: `uvicorn --workers 3`, 50 GET lintas worker → **0 kali 404**; 30 POST bersamaan → **30 sukses, 0 gagal**, 35 job tercatat utuh, tanpa `database is locked`). Ini dicatat karena CLAUDE.md sempat mengklaim sebaliknya, dan klaim itu **salah**: ditulis dengan mencocokkan pola kegagalan klasik `BackgroundTasks` (state disimpan di `dict` Python di memori → worker lain tidak bisa melihatnya → polling 404 acak) padahal produk ini **tidak begitu**. State-nya di SQLite, file di disk yang dibaca semua proses — itu sebabnya lintas-worker justru bekerja. **Pelajarannya: state di luar proses adalah yang membuat multi-worker mungkin, bukan mekanisme antriannya.** Yang belum diuji: banyak job PANJANG (~3 menit) berbarengan — job uji di atas gagal cepat di ingest, jadi tidak menahan thread lama. Kalau nanti diuji, batas yang lebih dulu tersentuh kemungkinan besar rate limit Anthropic, bukan worker-nya.
- **Dokumen tidak pernah dibersihkan.** `data/documents/` tumbuh selamanya — satu docx ~675 KB (terukur pada esteler sejak diagram 300 dpi; sebelumnya ~432 KB). Belum ada TTL, belum ada penghapusan job lama. Belum jadi masalah untuk pemakaian sekarang, tapi jadi masalah begitu ada yang memakainya rutin.
- ~~**Frontend masih form dasar tanpa penjelasan**~~ — **selesai 2026-07-16, dua tahap.** Tiap field kini menjelaskan dirinya (tiap klaim hint diverifikasi ke kode dulu), lalu di-redesign: bahasa visual dokumen (kertas/tinta/biru-dokumen, section bernomor seperti bab SDD), dan area status jadi log tahapan bercentang yang murni dari data server (tanpa mencocokkan string). Yang masih benar dari butir lama: prinsip non-teknis di *isi dokumen* datang dari system prompt LLM, bukan UI.
- ~~**Template `default` CRASH pada repo Nuxt/Next**~~ — **SELESAI 2026-07-18** (lihat Riwayat). Nama file dynamic-route (`[category]/[slug].vue`) merusak sintaks komponen PlantUML `[...]`; sekarang `_sanitize_route_param_brackets` di `_normalize_plantuml` melepas kurung route-param sebelum plantuml.jar, jadi diagram TETAP dirender (bukan placeholder) dan default tak lagi crash. Diverifikasi visual pada component_integration MyPertamina: pemetaan FE↔BE lengkap & benar. Batas yang tersisa & diterima: kalau LLM menghasilkan PlantUML rusak lewat jalur LAIN (bukan kurung route-param), diagram yang DIPAKAI template masih gagal-berisik — belum ada resiliensi per-diagram (placeholder), dan itu keputusan sadar (menjaga filosofi gagal-berisik). Repo Kotlin (Android PREMCO) tetap lemah — parser tak mendukungnya (lihat butir bahasa).
- **Template `premco` kini menyediakan SDD DAN UAT (UAT sejak 2026-07-18); gaya-nya terbukti netral-bahasa (Python + TS/JS), tapi FORMAT-nya masih dari SATU sumber template.** Diverifikasi menghasilkan dokumen berjejak pada esteler (Python/Flask) DAN MyPertamina.id-Clone (Vue/JS/TS, 2026-07-18) — jadi klaim "repo bahasa apa pun yang didukung → gaya premco" bukan lagi asumsi. Yang MASIH satu sumber: bentuk visual/struktur premco diukur dari satu dokumen PREMCO SDD **dan** satu dokumen PREMCO UAT (dari perusahaan yang template default kita pun dimodelkan darinya), jadi V2 (upload sembarang template) tetap menunggu 1-2 template docx dari sumber lain. Roadmap tahap (b) — gaya premco untuk UAT — SELESAI. Job juga tidak menyimpan `template_id` — cukup untuk sekarang karena docx-nya sudah jadi saat job selesai, tapi riwayat job tidak bisa menjawab "dokumen ini gaya apa".
- **Tidak ada hubungan/integrasi dengan project sibling `auto-project-tester`** — keduanya independen. Kalau menjalankan keduanya bersamaan secara lokal, perhatikan **keduanya sama-sama default ke port 8000** untuk backend-nya masing-masing — pastikan tidak salah port sebelum menyimpulkan sesuatu error/berhasil.

## Riwayat Perubahan Penting

- **2026-07-20** — **Fix panah use case menyilang: diagram use case DIPECAH per-aktor — di-SHIP $0, TANPA sentuh LLM/prompt/DocumentContent.** Lanjutan investigasi debug Flowy (yang mengira fix butuh fase-A IR / prompt berbayar). **Jalur KETIGA ditemukan & dipilih (kualitas terbaik + termurah)**: **parse PlantUML use case karangan LLM** (grammar dibatasi SYSTEM_PROMPT: `actor`/`rectangle`/`usecase`/`-->`) **→ `UseCaseDiagramIR` → render satu diagram BERSIH per aktor** lewat `PlantUMLRenderer` — semua di COMPILER, memakai-ulang output LLM yang ADA, jadi **$0 verifikasi** (tak ada panggilan LLM). Ini juga **dogfood layer Diagram IR** yang dibangun sesi ini: IR + `PlantUMLRenderer` jadi mesin re-layout-nya. Implementasi: `compiler_service._usecase_plantuml_to_ir` (parser toleran, None kalau tak cocok) + `_split_usecase_images` (< 2 aktor / parse gagal / render gagal → None); flag `_ResolvedTemplate.splits_usecase` (built-in `default`=True, `premco`=False, compiled=False); `_build_sdd_context(..., split_usecase=)` meng-emit `use_case_diagrams_by_actor` (list per aktor) + `use_case_figure_count` (offset penomoran gambar activity — hardcoded `+4` diganti `+3+count`); `sdd_template.md` use case jadi loop kondisional (per-aktor kalau ada, else diagram tunggal). **Fallback = NOL REGRESI**: <2 aktor / parse/render gagal → diagram tunggal seperti sekarang (dummy 1-aktor membuktikannya di 288 test lama). **Diverifikasi VISUAL $0** (Contract B 3-aktor → docx → PDF → PNG dilihat): Gambar 4/5/6 = Use Case per Admin/Approver/Requester, masing-masing **NOL panah menyilang**, activity lanjut Gambar 7, penomoran benar ujung-ke-ujung. **295 test hijau** (+7 `tests/test_usecase_split.py`: parse multi/garbage-tanpa-actor-atau-usecase, split multi/single-→None/render-fail-→fallback, context split-vs-single, flag default-True/premco-False). **Batas jujur**: template `default` DAN `premco` (diperluas ke premco sesudah default — offset penomoran gambar premco beda: use case Gambar 3..(2+K), activity `loop.index+2+count`; diverifikasi visual `premco_p12.png` — Gambar 3/4/5 per aktor, gaya bar-biru premco utuh); compiled/upload (hasil `POST /templates`) KINI juga memecah per-aktor — badan use case generator (`template_generator_service._BODY[USE_CASES]`) meng-emit loop kondisional + manifest `splits_usecase=True`; template LAMA tanpa flag → `manifest.get(...,False)` → tetap diagram tunggal (NOL regresi, badan lama tak punya loop); diverifikasi visual `upload_p1.png` (3 diagram per-aktor, gaya template-generate). Jadi ketiga jenis (default/premco/upload) memecah. Parser cuma menangani subset grammar LLM (di luar itu → fallback aman, bukan crash). **Pelajaran**: asumsi "butuh fase-A/berbayar" (di entri debug di bawah) KELIRU — reuse output LLM + re-layout di compiler memberi hasil terbaik pada $0; layer IR terbukti berguna nyata, bukan cuma fondasi. Probe: `split_p12.png` (scratchpad).

- **2026-07-20** — **Debug dokumen hasil user (repo Flowy, $0,33 normal): 2 keluhan dibedah lewat "lihat file-nya", 1 diperbaiki + 1 diinvestigasi.** Dokumen sehat (18 bab, isi koheren) tapi dua bug. **(1) "Logo tak muncul"** — ternyata file yang di-upload sebagai logo BUKAN logo, melainkan **SCREENSHOT aplikasi ini** (terbaca jelas: form generator, "Nama Project: Repo-Si-Indra", "Gagal: Repository tidak ditemukan") — user salah pilih file; dan karena gambarnya tinggi (1910×3856) ter-render **sliver ~0,22" tak terlihat** di header (sizing "tinggi 0,45 inci" untuk gambar portrait). **Fix ($0, di-commit)**: **preview thumbnail logo di frontend** (`.logo-preview` + `<img src={logo.base64}>`) — salah-pilih file kini ketahuan SEBELUM generate. **(2) "Panah use case tak jelas"** — DIKONFIRMASI nyata: diagram use case (3 aktor × ~13 use case, banyak use case dipakai bersama) panahnya menyilang-menumpuk di tengah — keterbatasan auto-layout `smetana` untuk diagram padat, bukan render gagal. **Investigasi $0**: Graphviz `dot` (layout lebih rapi) TAK terpasang → memakainya butuh dependency runtime baru (justru yang smetana dipilih untuk dihindari). Alternatif TANPA-Graphviz **pecah per-aktor** diuji & **terbukti jauh lebih bersih** — dan ini **dogfood layer Diagram IR**: rekonstruksi use case Flowy jadi `UseCaseDiagramIR` → render satu-diagram (reproduksi kusut, `uc_full.png`) vs 3 per-aktor (bersih, `uc_admin.png`) lewat `PlantUMLRenderer`+jar; layer IR yang dibangun sesi ini membuat investigasi ini trivial (beberapa baris). **Belum di-ship**: wiring per-aktor ke pipeline live butuh data use case TERSTRUKTUR (label oval pendek + aktor) — itu ranah **fase A IR** (LLM emit IR) atau prompt-change (berbayar verify); `use_cases[]` Contract B punya `actor` tapi labelnya "UC-xx + deskripsi panjang", bukan oval pendek seperti diagram. **Bonus temuan tak terkait (belum diperbaiki)**: LLM menamai app "Flowdesk" di narasi padahal `project_name` form = "Testing" (dipakai di judul/tabel) — SYSTEM_PROMPT tak pernah menyuruh LLM memakai `project_name` sebagai nama app, jadi ia mengarang; melanggar janji hint form ("AI memakainya"). Fix = aturan prompt (berbayar verify). Probe: `uc_full.png`/`uc_admin.png` (scratchpad).

- **2026-07-20** — **#3 EKSPOS V2 di frontend: widget upload template + dropdown gaya dokumen dari server. Fitur V2 kini BISA DIPAKAI lewat UI, bukan cuma API.** Sampai kini seluruh mesin V2 (upload → kompilasi → generate) tak terjangkau dari frontend: dropdown "Gaya Dokumen" hardcode `default`+`premco` (dan `premco` UAT ketinggalan diam-diam sejak backend mendukungnya 2026-07-18), tak ada widget upload. Frontend jadi bottleneck — pemilik dulu bilang "frontend cukup", tapi itu SEBELUM V2 jadi backend usable; ini ekspos FUNGSIONAL (bukan investasi desain), jadi "memang diperlukan" kini terpenuhi. **Backend (dukungan minimal untuk dropdown yang benar)**: `GET /templates` diperkaya dari daftar id string → `[{id, name, doc_types, source}]` (`compiler_service.list_templates_detail`) — server jadi sumber kebenaran doc_types per template, jadi frontend tak perlu (dan tak bisa lagi) hardcode; `premco` UAT otomatis benar. `list_template_ids` lama tetap ada (dipakai orkestrator kompilasi). **Frontend (`App.jsx`, $0)**: (1) `useEffect` fetch `GET /templates` saat mount → dropdown gaya diisi dari server, difilter per `documentType` (`availableTemplates`); `TEMPLATE_OPTIONS` hardcoded DIHAPUS. (2) **Widget upload** di panel Dokumen: input `.docx` + checkbox "petakan dengan AI (berbayar)" (`use_llm_mapping`) + tombol → `POST /templates` multipart → sukses: refresh daftar, auto-pilih template baru, pindah `documentType` kalau template tak mendukung yang terpilih. (3) **Ringkasan hasil peta** langsung setelah upload (`summarizeMapping`: berapa bab terisi dari kode) — kalau 0, UI menyarankan centang AI lalu upload ulang, **menyurfacing nilai #2 tepat di titik keputusan**. Kontrak respons `POST /templates` (`{manifest, mappings}`) sudah pas dengan yang dikonsumsi frontend (`manifest.template_id`/`name`/`doc_types`, `mappings`). **Tes**: `GET /templates` shape diuji (`test_routes_template`: doc_types/source built-in + entry compiled), upload + llm-opt-in lewat TestClient (aplikasi ASGI NYATA) — **288 hijau**, nol regresi. Frontend build bersih (`vite build`). **Batas jujur**: UI belum di-klik-uji live (tak ada harness browser di sesi ini) — diverifikasi lewat build + tes endpoint + tinjauan kode; jalur HTTP-nya identik yang diuji TestClient. **Sisa V2**: UI tinjauan/EDIT peta bab sebelum generate (tampilkan `mappings`, user sunting binding lalu generate), orientasi landscape per-section, job simpan `template_id`.

- **2026-07-20** — **#2 PRODUKSIONISASI peta bab LLM: prototipe scratchpad → kode `app/` ber-tes + gate biaya opt-in. Template SDD asing kini bisa BERISI, bukan placeholder.** Lanjutan langsung prototipe hari ini (yang buktikan LLM 0→4 binding pada Dynamics). Sekarang modul nyata: **`app/services/llm_mapping_service.py`** `llm_propose_mapping(spec, doc_type)` — kontrak keluaran SAMA dengan heuristik `propose_mapping` (`[{level,text,binding}]`), jadi **dipertukarkan**. **Refactor pendukung (satu sumber kebenaran)**: logika dedup isi + degradasi anggun + SKIP-judul diekstrak jadi `template_generator_service.assemble_plan(outline, classify)`, dipakai BERSAMA heuristik & LLM — supaya dua pemeta tak menyimpang diam-diam (pelajaran yang sama dengan `format_contract_a`/`_join_url_path`). Heuristik kini cuma menyediakan `classify` berbasis kata kunci; LLM menyediakan `classify` dari jawaban Claude; sisanya seragam. **Gate biaya**: `compile_template(..., use_llm_mapping=False)` — DEFAULT heuristik $0 (perilaku + 200+ test lama TAK berubah, dibuktikan); `True` = satu panggilan Claude per (template × doc_type), terjadi **SEKALI saat kompilasi template, BUKAN tiap generate dokumen** (mapping di-bake ke template Jinja tersimpan). Endpoint `POST /templates` menerima `use_llm_mapping` (Form, default false). **Tiga safety**: (1) binding isi difilter per doc_type (`_SDD_CONTENT`/`_UAT_CONTENT`, cermin `_SDD_RULES`/`_UAT_RULES`) — LLM tak bisa menaruh test-case di SDD atau diagram arsitektur di UAT; (2) dedup first-wins via `assemble_plan` kalau LLM langgar "sekali per binding"; (3) enum structured-output `_Binding` dijaga tes agar tak menyimpang dari konstanta binding. **Tes +11 ($0, LLM di-mock di boundary `_request_bindings`)**: `tests/test_llm_mapping_service.py` (pemetaan bab asing, filter doc_type dua arah, dedup, fallback kontainer/daun, enum-drift), wiring opt-in di compiler & endpoint, plus test heuristik lama HIJAU (refactor behavior-preserving). Full suite **288 hijau**, nol regresi. **Diverifikasi jalur BERBAYAR asli** (~$0,03, Sonnet 5, sekali — modul produksi ≠ prototipe, perlu bukti terhadap API nyata): `llm_propose_mapping` pada `Template SDD` Dynamics = **6 binding isi** (Vision Statement→app_description, Business Capabilities→feature_requirements, Business & IT Alignment→business_flow, MS Dynamics Standardize→system_requirements, 2× Solution Design→architecture/use_cases) — vs heuristik 0. **Urutan keputusan (jawaban ke pemilik "bagusnya #2 atau #3?")**: #2 dipilih dulu karena substansi sebelum etalase — meng-ekspos V2 di frontend (#3) tanpa #2 memajang fitur yang menghasilkan dokumen kosong untuk template asing; #2 juga unblocked (backend, tak perlu restu frontend) & biayanya per-upload bukan per-generate. **Sisa V2**: UI tinjauan/edit peta (frontend, $0), ekspos V2 di frontend (#3, butuh restu "frontend cukup"), orientasi landscape per-section.

- **2026-07-20** — **Redesign layer diagram: Diagram IR (representasi semantik netral) + PlantUML Renderer, sebagai modul BARU `app/diagram/` — TANPA menyentuh pipeline live (DOCX identik).** Permintaan pemilik: beri sistem diagram arsitektur ala compiler — **IR = AST, PlantUML cuma SATU backend renderer** (SVG/Mermaid/drawio/ReactFlow kelak), dengan larangan tegas menyentuh parser/Contract A/pipeline LLM/generator DOCX. **Kontradiksi inti dibedah SEBELUM kode** (pola "debat di awal"): "IR jadi source of truth" menuntut IR diproduksi SEBELUM PlantUML — artinya LLM meng-emit IR, yang **mengubah DocumentContent + prompt (dilarang) DAN mengubah output DOCX (bukan "identik")**. Sebab hari ini **LLM menulis PlantUML LANGSUNG ke DocumentContent** (`diagrams.system_architecture`/`component_integration`/`use_case_diagram`/`business_process_flow` + `activity_diagrams[].diagram_script` = string PlantUML karangan LLM; compiler `_build_sdd_context` cuma me-render-nya) — jadi "PlantUML source of truth" itu harfiah. Ketiga tuntutan ("IR source of truth" + "DOCX identik" + "jangan ubah LLM/DocumentContent") **mustahil sekaligus** (paling banyak dua dari tiga). Pemilik memilih **Option C**: bangun IR + renderer sebagai fondasi SIAP-PAKAI, live pipeline tak disentuh (DOCX identik karena renderer baru TAK dipanggil jalur live), dan "IR jadi source of truth" (deliverable asli #3) **DI-STAGE** ke fase berikutnya (Option A: LLM emit IR via structured output — mekanisme Pydantic yang codebase SUDAH pakai). **Desain**: IR = **graf** (node/edge/lane), BUKAN blok-terstruktur ala PlantUML — sengaja, karena graf memetakan LANGSUNG ke Mermaid/ReactFlow/drawio; justru PlantUML activity yang "aneh" (butuh rekonstruksi if/else dari graf), yang menegaskan PlantUML cuma satu backend. Per-tipe kelas Pydantic (`ActivityDiagramIR`/`UseCaseDiagramIR`/`ArchitectureDiagramIR`/`ComponentDiagramIR`) berbagi `DiagramIR` base; Pydantic dipilih supaya IR siap jadi skema structured-output LLM di fase A. `DiagramRenderer` (ABC) + `PlantUMLRenderer` mendispatch per `diagram_type`. **Kunci "switch-ready"**: renderer meng-emit PlantUML **STRUKTUR-saja tanpa `!theme`/`skinparam`** — persis yang di-emit LLM — jadi masuk ke `_normalize_plantuml`→`_run_plantuml` yang SAMA (gaya disuntik di sana, filosofi reference.docx). **Diverifikasi VISUAL ($0)**: IR arsitektur & use case → `PlantUMLRenderer` → jalur render jar ASLI → PNG dilihat = UML bergaya IDENTIK jalur LLM (aktor stick-figure, silinder DB, oval use case dalam rectangle sistem). Bonus: component renderer pakai alias id (`component "X" as id`), BUKAN `[Label]` — kebal bug kurung route-param yang pernah meng-crash jalur LLM. **Cakupan renderer activity pertama (jujur)**: linear + keputusan if/elseif/else ke terminal (grammar activity produk hari ini); reconvergence & loop `repeat/while` = perluasan renderer, BUKAN batas IR (IR sudah bisa mewakilinya lewat edge/MERGE — backend graf murni tak butuh langkah ini sama sekali). **Tes**: +14 (`tests/test_diagram_ir.py`, termasuk contoh activity PERSIS dari brief pemilik), full suite **277 hijau** (263+14, NOL regresi — cuma menambah file, `app/` lama tak disentuh). Biaya **$0**. **Menambah SVG kelak** = tambah `renderer/svg/SVGRenderer(DiagramRenderer)` yang mengonsumsi IR yang sama — nol perubahan IR maupun pipeline. **Sisa (fase A, TERPISAH + butuh restu karena melanggar constraint sekarang)**: LLM emit IR → DocumentContent.diagrams jadi IR → IR jadi source of truth → renderer dipilih via config; output DOCX berubah (perlu verifikasi ulang + regen berbayar). Probe = scratchpad (`IR_arch.png`, `IR_usecase.png`).

- **2026-07-20** — **3 template vendor yang belum dijalankan diuji lewat mesin V2, lalu peta bab LLM di-PROTOTIPE-kan ($0,034) — plafon heuristik TERBUKTI sekaligus TERATASI.** [Konteks: demo batal → kembali ke Trek A "kumpulkan lebih banyak template".] Ketiga template — `04` UAT (keluarga-PREMCO), `moe` UAT (asing, ex-`.doc` dikonversi Word COM), dan `Template SDD` (template Microsoft Dynamics CRM yang pemilik tambahkan HARI ITU) — **kompilasi+render tanpa crash** lewat `compile_template_from_docx` → `generate_docx` dgn Contract B nyata: fix subtree-drop/dedup sesi lalu **tahan pada outline bersarang L0-L7 asing** (bukan sintetis). Temuan crux dari `Template SDD`: `propose_mapping` menghasilkan **0 binding isi dari 29 bab** — dokumen 100% `*(diisi manual)*` (dilihat: dua halaman penuh placeholder). **Bukan bug — PLAFON heuristik**: nama bab spesifik-domain (Vision Statement, Business Capabilities, Data Master Design, Opportunity to Sales Order Process) tak cocok satu pun kata kunci `_SDD_RULES`, jadi degradasi anggun menelan SEMUANYA. Kasus terkuat sejauh ini untuk peta LLM: IEEE (sesi lalu) masih dapat ~3-4 binding, Dynamics dapat 0. **Prototipe peta LLM ($0,034, Sonnet 5, structured output, KONTRAK KELUARAN SAMA `[{index,text,binding}]`, enum binding dibatasi + aturan dedup "sekali per binding")** membuktikan teratasi: **4 binding isi** (Vision Statement→app_description, Business Capabilities→feature_requirements, Business & IT Alignment→architecture, MS Dynamics Standardize→system_requirements) PLUS memperbaiki **3 kesalahan STRUKTURAL** heuristik (judul dokumen→skip, "1 Contents"→skip [TOC yang `_SKIP_KEYWORDS` lewatkan karena bukan persis "table of contents"], "PT ABC"→skip). Dilihat before/after: dokumen 12KB semua-placeholder → 27KB berisi paragraf deskripsi + tabel System Requirement (Python/FastAPI/PostgreSQL) + **diagram arsitektur ter-render** + tabel fitur, sementara bab yang MEMANG manual (Change Record, Setup, Data Master detail) TETAP placeholder jujur — LLM menunjukkan JUDGMENT, setia nilai jual "isi yang bisa, jujur untuk sisanya, jangan karang". Dedup safety first-wins tak terpicu (LLM patuh). **Artinya "LLM auto-usul peta bab" (satu-satunya sisa V2 non-deterministik/berbayar) kini DE-RISKED — tersisa PRODUKSIONISASI (jadikan opsi di `template_compiler_service` di belakang gate biaya + tes), bukan lagi kelayakan.** Terkonfirmasi ulang masih perlu: **landscape per-section** (tabel test 7-kolom `04` patah mid-kata di potret; `orientations` bahkan terukur `None` di ketiga spec — pengukuran + penerapan sama-sama belum jalan). Semua $0 kecuali prototipe $0,034. **Batas jujur**: masih 1 template tiap kelas struktur, dan prototipe LLM mengisi Contract B DUMMY (membuktikan MESIN peta jalan pada template asing, BUKAN repo nyata mengisinya). Trek A (lebih banyak template sumber lain) tetap #1 selagi magang. Probe = scratchpad (bukan repo).

- **2026-07-19** — **Mesin V2 diuji END-TO-END pada template VENDOR ASLI multi-sumber (pertama kali lewat pipeline app, bukan spec sintetis) — dan template nyata langsung memancing DUA cacat heuristik yang uji sintetis tak pernah munculkan.** Pemilik menyediakan template asli di root (gitignore): SDD gaya IEEE/CMS (`.doc`, EN, keluarga rekayasa-standar — PALING jauh dari PREMCO), UAT ID (`04`, `.docx`, keluarga PREMCO sumber beda), UAT EN generik (`.doc`). `.doc` dikonversi ke `.docx` via Word COM lalu dijalankan lewat `compile_template_from_docx`. **Increment 1 (`build_template_spec`) GENERALISASI**: ketiga template terukur benar — IEEE = Times New Roman/Arial, header abu `D9D9D9`, EN, landscape; moe UAT = Arial, header `None`; semua ≠ PREMCO (Calibri/hitam). **`propose_mapping` sebagian besar generalisasi + degradasi anggun TERKONFIRMASI pada IEEE**: dari 81 bab, ~13 dapat isi turunan-kode (Introduction/Background/Overview→app_description, Architecture→diagram, Business Process→flow, Functional Requirements→features), sisanya → **placeholder jujur** (Data Design, Detailed Design, Human-Machine Interface, Scope, Purpose — bab IEEE mustahil dari kode). **Nol karangan, nol crash.** moe UAT: 3 bab test-script (Form-Based/Business Process/Report) benar → `test_groups`. **DUA CACAT ditemukan — dan HANYA muncul karena template nyata BERSARANG DALAM (outline uji sintetis selalu DATAR/L1)**: **(A) DUPLIKASI** — bab bersaudara yang cocok isi sama mengulang isinya (Introduction & Background dua-duanya → app_description → paragraf IDENTIK, terlihat di render). **(B) SUBTREE-DROP terlalu rakus** — `_OWNS_SUBTREE` memuat binding isi SKALAR, jadi bab konten top-level menelan SELURUH sub-pohonnya: bab anak yang punya pemetaan sendiri IKUT HILANG (`Background`→app_description menelan `Overview of Business Process`→business_flow) dan struktur bab template (Scope, Purpose) ikut terbuang. **Keduanya diperbaiki ($0)**: (A) dedup di `propose_mapping` — tiap binding isi dipakai paling banyak SEKALI (yang pertama menang; ulangan → placeholder/kontainer); (B) `_OWNS_SUBTREE` dipersempit ke HANYA binding yang meng-emit sub-heading sendiri (`use_cases`/`activity_diagrams`/`test_groups`) — isi skalar tak lagi menelan anak. **Diverifikasi VISUAL before/after pada IEEE** (PROBE31): sebelum = 26 KB/6 hlm, app_description **2×**, sub-bab hilang; sesudah = 63 KB/9 hlm, app_description **1×**, "Introduction" diikuti sub-babnya (Purpose/Identification/Scope/Relationship/Methodology) sebagai placeholder jujur — **cocok struktur IEEE**, persis janji V2 "generate mengikuti template ANDA". **263 test hijau** (+2: dedup first-wins, isi skalar tak menelan sub-pohon). Biaya: **$0**. **Pelajaran**: uji sintetis (outline datar) BUTA terhadap sekelas bug bersarang — kembaran "fixture N=1 buta ke bug antar-item" & "satu repo membuktikan fix JALAN, repo kedua membuktikan GENERAL". Input vendor nyata memunculkan keduanya. **Batas jujur**: ini isi Contract B DUMMY dirender lewat STRUKTUR template asing — membuktikan mesin ukur+peta+sintesis jalan pada template asing, BUKAN bahwa repo nyata mengisinya (itu jalur produk penuh, $0 diuji nanti). Cacat mapping yang tersisa (bab MANA yang paling pas untuk tiap isi — mis. "Overview of the System" mungkin lebih pas app_description daripada "Introduction") butuh judgment = tugas peta LLM; heuristik "yang pertama menang" adalah stand-in.

- **2026-07-19** — **V2 endpoint upload: `POST /templates` — user meng-upload `.docx` template, langsung dapat `template_id` yang bisa dipakai di `/documents/generate`. Fitur V2 kini BISA DIPAKAI, bukan cuma lewat kode.** `app/api/routes_template.py` (router baru, didaftar di `main.py`). **SINKRON** — beda dari `/documents/generate` yang async: kompilasi template deterministik & cepat ($0, tanpa LLM), jadi tak perlu job/polling; balik **201** langsung. Multipart: `file` (wajib), `name`/`doc_types` (opsional Form). Alur: tulis upload ke temp → `compile_template_from_docx` (ukur→generate→sintesis→simpan→daftar) → balik `{manifest, mappings}` — `mappings` = rencana peta bab `{DOC: [{level,text,binding}]}` supaya user bisa LIHAT bagaimana tiap bab dipetakan (bahan UI tinjauan kelak). **Pemisahan error yang penting**: `.doc`/non-`.docx`/kosong/rusak = **422** (`PackageNotFoundError`/`BadZipFile`/`KeyError` → kesalahan INPUT user), kebesaran (>50 MB) = **413**, tapi kegagalan sisi SERVER (mis. pandoc tak terpasang saat sintesis reference) = **500** — jangan salahkan file user untuk masalah server (pelajaran berulang: sebab tertelan gejala). `.doc` ditolak jelas ("simpan ulang sebagai .docx") karena konversi butuh Word, tak ada di deployment. Dua endpoint pendukung: `GET /templates` (semua id built-in+upload, untuk dropdown — pakai `list_template_ids()` yang dibikin publik sebelumnya) & `GET /templates/{id}` (detail+mappings, untuk UI tinjauan; 404 kalau bukan hasil-upload). **Payoff tersambung tanpa kerja tambahan**: `/documents/generate` sudah memanggil `validate_template(template_id)` sinkron (422 sebelum job), dan `validate_template` sejak pendaftaran+storage menerima id terkompilasi — jadi id hasil-upload otomatis sah di generate. **261 test hijau** (+5 `tests/test_routes_template.py`, TestClient=aplikasi ASGI nyata: upload→compile→terdaftar→lolos validate_template, tolak .doc/non-docx/garbage/kosong, list memuat built-in, 404 id tak dikenal). Biaya: **$0**. Sisa V2: LLM auto-usul peta bab (berbayar), UI tinjauan+widget upload di frontend, orientasi landscape per-section.

- **2026-07-19** — **V2 pendaftaran + storage: template hasil-upload kini benar-benar bisa didaftarkan & DIRENDER lewat `generate_docx` — jalur produksi penuh terbukti VISUAL.** Menutup rantai V2: increment 1-3 memproduksi artefak template; langkah ini membuatnya jadi `template_id` nyata yang pipeline matang render. **Enabler (refactor murni)**: logika sintesis reference.docx DIPINDAH dari `scripts/build_reference_docx.py` ke **`app/services/reference_synthesis_service.py`** (`build_reference(dest, spec)`) — runtime butuh memanggilnya, dan script→app adalah arah impor yang benar (script = alat dev yang memakai app). Script kini pembungkus CLI tipis; guard content-identity yang ada memverifikasi pemindahan tak mengubah output (build(spec=None) tetap content-identik reference ter-commit). **Storage**: `data/templates/<id>/` (persisten, gitignored lewat `data/` — sama alasannya dengan `jobs.db`; `config.TEMPLATES_STORE_PATH`) berisi `template.json` (manifest), `<DOC>.md` (template Jinja hasil-generate per doc_type), `reference.docx` tersintesis, `spec.json`, `mapping_<DOC>.json` (rencana peta, untuk UI tinjauan kelak). **Registry dinamis**: `_resolve_template(id, type) -> _ResolvedTemplate` (dataclass) menyatukan built-in & terkompilasi jadi satu deskriptor: sumber Jinja (`get_template` untuk built-in supaya output byte-identik; `from_string` untuk terkompilasi), path reference.docx, dan **flag perilaku** (`uses_component_integration`/`groups_test_cases`/`uat_toc`). Flag ini menggantikan cek `template_id == "premco"`/set-membership yang dulu tersebar di `_build_sdd_context`/`_build_uat_context`/`_pandoc_args` — sekarang built-in mendapat flag dari peta kecil, terkompilasi dari manifest; `validate_template` (jalur 422 sinkron) & `list_template_ids` (publik, juga untuk dropdown frontend) mencakup terkompilasi. **Orkestrator** `template_compiler_service.compile_template(spec, name)` menyatukan increment 1-3: generate template per doc_type + sintesis reference + tulis manifest + daftar; `compile_template_from_docx` menambah ukur (`build_template_spec`) di depan. Persis kerja MANUAL pembuatan `premco` (V1), diotomatiskan. **Keputusan yang layak diingat**: (1) orkestrator mereferensi `compiler_service.TEMPLATES_STORE` lewat MODUL, bukan `from ... import` (binding by-value) — supaya store punya SATU sumber kebenaran yang bisa di-redirect (test/config) di satu tempat; (2) built-in tetap `get_template` (bukan `from_string(read_text)`) supaya output byte-identik — nol risiko regresi 200+ test; (3) flag `groups_test_cases`/`uat_toc`/`uses_component_integration` di manifest = jawaban atas "warna tabel spec-driven" & "diagram integrasi" untuk jalur generate tanpa marker. **Diverifikasi VISUAL jalur PRODUKSI PENUH $0** (PROBE30): `compile_template` → `acme-sdd-template` terdaftar bersama default/premco → `compiler_service.generate_docx("SDD", contract_b, template_id="acme-sdd-template")` dengan **diagram PlantUML ASLI** (bukan mock) → docx → PDF → PNG → dilihat: judul/bab Tahoma-kiri, tabel header-abu spec-driven, konten Contract B nyata, **diagram arsitektur User→Frontend→Backend→Database ter-render**. **256 test hijau** (+4 `tests/test_template_compiler_service.py`: artefak+registrasi, render lewat reference tersintesis-sendiri [tema Tahoma bukan PREMCO Calibri], id unik, jalur dari-docx). Biaya: **$0**. Sisa V2: **endpoint upload** template (belum ada API untuk meng-upload docx & memicu kompilasi — orkestrator siap dipanggil), LLM auto-usul peta [berbayar], UI tinjauan, orientasi landscape per-section.

- **2026-07-19** — **V2 increment 3 (INTI deterministik) jadi kode ber-tes: generator template Jinja dari outline + peta bab heuristik — dan rantai spec→peta→template→isi→render terbukti VISUAL pada kode app.** Lanjutan langsung increment 2. `app/services/template_generator_service.py`, dua fungsi sengaja dipisah agar bisa ditinjau manusia di antaranya: **`propose_mapping(spec, doc_type)`** → rencana `[{level, text, binding}]` untuk tiap bab outline (berbasis kata kunci, deterministik — **stand-in usulan LLM**; bentuknya memang untuk UI tinjauan), dan **`generate_jinja_template(mapping)`** → string template Jinja mengikuti urutan bab. Bab isi-turunan-kode di-bind ke loop/tabel Contract B (`app_description`/`user_roles`/`system_requirements`/`feature_requirements`/`use_cases`/`activity_diagrams`/`architecture`/`business_flow`/`test_groups`); bab yang tak dikenali → **degradasi anggun**: kontainer (punya sub-bab) jadi `heading_only`, daun jadi `*(diisi manual)*` — placeholder JUJUR, bukan karangan (setia nilai jual "30 halaman benar > 87 halaman separuh karangan"). Bab yang isinya diganti turunan-kode MEMBUANG anak-anak bab yang di-upload (sampel sub-bab template digantikan loop Contract B — sama seperti prototipe). **Keputusan penting**: (1) snippet Jinja **DIAMBIL dari template `default` yang sudah terbukti render**, bukan ditulis baru — jadi markup pasti valid di bawah `trim_blocks`/`lstrip_blocks` compiler, termasuk baris-kosong-sebelum-heading/list (blank_before_header — pelajaran lama: tanpa itu kriteria/heading bocor jadi teks literal, kelas bug yang pernah menggigit use case esteler); (2) **warna header tabel TANPA marker** (`((GH))` dsb.) — untuk template hasil-generate, warna datang dari `tblStylePr firstRow` reference.docx tersintesis yang **sudah spec-driven** (increment 2), jadi "warna tabel spec-driven" beres otomatis untuk jalur generate (marker premco hanya soal template buatan-tangan); (3) peta heuristik sengaja **konservatif** (nama tak dikenali → placeholder, tak menebak) — LLM-lah yang kelak memetakan bab asing dengan judgment, modul ini menyediakan kontrak deterministik yang dia isi. **Diverifikasi VISUAL end-to-end $0** (PROBE29): outline SDD sintetis → `propose_mapping` (peta benar: judul/TOC skip, isi ter-bind, "Mockup" → manual) → `generate_jinja_template` → isi `document_content_sdd.json` nyata → render lewat reference-`04` tersintesis → docx → PDF (Word COM) → PNG → **dilihat**: dokumen SDD lengkap gaya `04` (bab Tahoma-kiri, body Verdana, tabel header-abu spec-driven berisi konten Contract B nyata Python/FastAPI/PostgreSQL, use-case & activity ber-sub-heading `###` benar, Acceptance Criteria list bernomor tanpa kebocoran, Mockup jadi placeholder jujur). **252 test hijau** (+6 `tests/test_template_generator_service.py`: pemetaan kata kunci SDD/UAT, degradasi anggun, pembuangan anak bab, DAN smoke render template hasil-generate dgn Contract B nyata di env Jinja compiler — nol tag Jinja tersisa). Biaya: **$0**. Sisa increment 3 (checkpoint berikut, sebagian berbayar): pendaftaran ke `_TEMPLATE_REGISTRY` + penyimpanan artefak per-upload (butuh desain storage + override reference.docx per-template di `generate_docx`), **LLM auto-usul peta bab** (satu-satunya bagian non-deterministik/berbayar), UI tinjauan pemetaan, dan sintesis orientasi landscape per-section (spec `orientations[]` sudah diukur increment 1, belum dipakai).

- **2026-07-19** — **V2 increment 2 SELESAI jadi kode ber-tes: sintesis reference.docx dari `TemplateSpec` — dan "byte-identik" ternyata istilah yang salah.** Lanjutan implementasi Trek B (increment 1 `TemplateSpec` sudah jadi `app/services/template_spec_service.py` sesi sebelumnya). Pendekatan A (diputuskan pemilik): perluas `scripts/build_reference_docx.py` dengan param `spec=None` alih-alih menyalin reference.docx ter-commit — satu sumber kebenaran, dan `spec=None` mereproduksi PREMCO dari kerangka Pandoc yang sama. **Temuan metodologis: rencana menuntut "`spec=None` byte-identik PREMCO", tapi diukur empiris itu MUSTAHIL & bukan yang diinginkan** — `build()` yang sama, dijalankan dua kali, menghasilkan raw byte BERBEDA karena **timestamp zip** (python-docx menulis waktu-sekarang ke tiap member); yang benar-benar invariant adalah **isi tiap member zip**. Semua member content-identik, cuma timestamp beda — wajar, tak berbahaya. Jadi guard test membandingkan **isi per-member** (`zipfile` → dict nama→byte), bukan byte kontainer. Kelas kesalahan yang sama dengan menguji XML mentah dulu: menguji **proksi** (byte kontainer) alih-alih **barangnya** (isi dokumen). Override sumber-spesifik dari spec, HANYA yang diminta: (1) tema major≠minor terpisah — `_set_theme_fonts` kini mengganti dua `<a:latin>` **berurutan** (major=heading dulu, minor=body; saat sama → output lama identik); (2) Title/Heading{1,2,3} {size,bold,caps,align} + TOC Heading mengikuti H2; (3) warna fill+teks header tabel — `_contrast_text` (YIQ, ambang 128) menghitung teks kontras (fill gelap→putih, terang→hitam) alih-alih mengukur per-dokumen. `char_spacing` **diturunkan dari `caps`** (`_letterspacing`: renggang 10 hanya bila huruf-besar) — satu aturan yang mereproduksi PREMCO byte-identik (H1/H2 caps→renggang, H3/Title non-caps→tidak) tanpa menambah knob. Field spec kosong (None/"") jatuh ke PREMCO; `spec={}`/`{"visual":{}}` → PREMCO utuh (jalur "template miskin" V0). **Diverifikasi VISUAL A/B di Word sungguhan** (docx→PDF via COM→PNG→dilihat, $0, PROBE28): konten IDENTIK, dua identitas — PREMCO Calibri/judul-tengah/bab-CAPS-renggang/header-hitam-teks-putih vs spec-`04` Tahoma/judul-kiri/bab-campuran-tak-caps/Verdana-body/header-abu-`A4A4A4`-teks-hitam. **246 test hijau** (+5 `tests/test_build_reference_docx.py`: content-identity guard, override spec-driven, fallback spec kosong, kontras dua arah). Biaya: **$0**. Tersisa untuk V2: increment 3 (generator template Jinja dari outline + `_TEMPLATE_REGISTRY`), LLM auto-usul peta bab, UI tinjauan, orientasi per-section.

- **2026-07-18 (sesi baru, lanjutan)** — **V2 (upload template sembarang) DE-RISKED: kelayakan terbukti end-to-end pada template multi-sumber, $0 — dan riset menutup gap yang V0 sengaja tinggalkan.** Pemilik membawa 5 dokumen (memenuhi permintaan V0: *"generalisasi butuh 1-2 template dari sumber lain"*). **Pemulihan dulu**: sesi UAT premco sebelumnya laptop MATI sebelum commit — kerjanya utuh di working tree (Edit menulis file utuh, jadi yang tersimpan lengkap), diverifikasi 239 test hijau lalu di-commit ulang (`92e9a77`); `*.doc` ditutup gitignore (dokumen vendor jangan ikut). Dari 5: **3 template isian** (SDD gaya IEEE/CMS EN, UAT ID keluarga-PREMCO, UAT EN generik) + **2 acuan** (standar IEEE 1016-2009 itu sendiri, panduan UAT). `.doc` biner dikonversi ke `.docx` via Word COM (python-docx tak baca `.doc`). **Temuan crux (menjawab pertanyaan overfit)**: paruh **sintesis visual GENERALISASI** — tiap properti yang V0 ukur pada PREMCO terukur mekanis di kelima berkas, nilainya liar (5 warna header beda: hitam/hijau/abu-D9/abu-A4/biru) tapi pengukurannya tak peduli nilainya apa; paruh **pemetaan isi TIDAK** — Contract B kita berbentuk PREMCO. Bukti dua-outline: **UAT ID hampir = template `premco` UAT** (bab "Detail Testing" = `Login/Logout/Menu→Submenu` = grouping per-layar kita → pemetaan nyaris penuh, keluarga sama sumber beda); **SDD IEEE/CMS = degradasi anggun** (dari 10 bab cuma ~3-4 turunan-kode: Background→`app_description`, System Architecture→diagram, External Interface→endpoint, System Integrity→security; Data Design/Detailed Design/Human-Machine Interface → **placeholder jujur**, dilihat manusia di layar tinjauan). Jadi fork "luas vs fokus" **BUKAN biner** — satu mesin, keluarga enterprise-app paling kaya, template asing placeholder jujur (setia nilai jual). **Keputusan arsitektur: X = kompilasi upload jadi template terdaftar** (bukan swap `--reference-doc` runtime yang V0 buktikan corrupt) — pakai ulang seluruh pipeline matang (Contract B→Jinja→Pandoc→reference.docx, 239 test), LLM cuma untuk pemetaan, manusia tinjau. **3 increment terbukti sebagai SATU RANTAI ($0, riset scratchpad)**: (1) **`TemplateSpec`** kontrak JSON (ukur docx→struktur: font/heading/warna-header/orientasi/outline/dekay). Pelajaran "bug Aptos" BERULANG: font "efektif" WAJIB di-resolve (`04` set `docDefault=theme:minorHAnsi` tapi style `Normal` override ke **Verdana**; tema IEEE=Aptos tapi override ke Times/Arial) — baca satu sumber menyesatkan; `caps` juga terukur, bukan diasumsikan PREMCO. (2) **Sintesis reference.docx dari spec** — SALIN reference.docx (warisi dot-leader/footer/padding/updateFields/caption) lalu OVERRIDE cuma sumber-spesifik (tema major/minor TERPISAH=font heading vs body, Title/Heading size/bold/caps/align, warna fill+teks header; teks header dari kontras luminance bukan diukur). Dibuktikan render: konten sama, dua reference → `04` Tahoma-kiri/Verdana/header-abu vs PREMCO Calibri-caps-tengah/header-hitam. (3) **Prototipe loop penuh pada `04`** — outline→**generate template Jinja** (peta heuristik keluarga-PREMCO, stand-in peta yang kelak diusulkan LLM; "Detail Testing"→loop `uat_test_groups`, sampel Menu/Submenu di bawahnya DIBUANG)→isi Contract B UAT→render lewat reference-`04` → dokumen `04`-style lengkap, test case nyata dikelompokkan per-modul. **Cacat satu-satunya = header tabel 7-kolom patah di potret, JUSTRU mengonfirmasi kenapa `04` memakai LANDSCAPE** (sengaja ditunda di prototipe): pengukuran memprediksi, potret membuktikan — bukan kegagalan loop. Laporan durable `scripts/validation/V2_TEMPLATE_MULTISOURCE.md`; PROBE26 (loop penuh `04`), PROBE27 (A/B tipografi). **Batas jujur**: semua riset scratchpad, belum kode `app/` ber-tes; N=3; sisa = IMPLEMENTASI (konsolidasi ke `app/`+tes fixture sintetis, LLM auto-usul peta untuk template asing, UI tinjauan, sintesis landscape/orientasi per-section, warna tabel khusus spec-driven karena marker hijau premco kini bertabrakan dgn spec). **Prioritas selagi magang** (rekomendasi ke pemilik): kumpulkan lebih banyak template sumber lain — kelayakan sudah terbukti, yang menaikkan keyakinan generalisasi sekarang = keragaman input, bukan lebih banyak kode; implementasi kerja repo-only yang TAK kedaluwarsa.

- **2026-07-18** — **TEMPLATE `premco` UAT: roadmap tahap (b) SELESAI — dimodelkan dari dokumen UAT PREMCO asli, dan MENGUKUR-nya (bukan menebak) langsung menyelamatkan dari kirim potret.** Pemilik membawa dokumen UAT Pertamina (`2. UAT_...Phase 2.docx` di root, gitignore otomatis lewat `*.docx`). Dikerjakan seperti SDD premco: buka & ukur dokumennya dulu (`python-docx`), baru tulis kode. Pengukuran mengungkap UAT sangat beda dari SDD-nya: **(1)** struktur FLAT — nol Heading 1/2/3, hanya Subtitle+Normal, label bab = paragraf bold; **(2)** NOL field TOC (tak ada Daftar Isi); **(3)** tabel test-case 9 kolom ber-header **HIJAU `a8d08d`** (bukan hitam/biru SDD; warna terukur dari 40+ tabel); **(4)** test case DIKELOMPOKKAN per-layar (~40 grup "Case Pengujian: Halaman X"), bukan satu tabel; **(5) yang paling menentukan — section Case Pengujian LANDSCAPE** (diukur: section 2 orientasi landscape, tabel 10,9 inci). Tanpa mengukur, potret dengan header patah-patah ("N o", "Kegiata n") akan terkirim — "buka dokumennya, jangan menebak" bekerja lagi. **Yang dibangun ($0):** `uat_premco_template.md` (flat, Document Information 5-baris, Distribution From/To, Sertifikasi L/G, Prosedur+Entry/Exit/Formulir+Status+Kode Kegagalan A-D — boilerplate sebagian sudah ada di `uat_template.md` yang memang ditulis dari dokumen ini dulu; typo asli "kesalah desain" DIBERSIHKAN, bukan disalin — pelajaran V0 "bersihkan, jangan salin pembusukan"); compiler: registry premco→UAT, `_pandoc_args` lewati `--toc` untuk premco UAT, `_build_uat_context` grouping per `module` (fallback SATU tabel kalau tak ada module — toleran untuk Contract B lama), marker `((GH))` → `_apply_green_headers` (hijau + matikan header hitam per-tabel, marker-driven supaya template lain tak tersentuh), reuse `((BR))` untuk langkah multi-baris, dan `_landscape_after_marker` (marker `((LANDSCAPE))` → pecah section: front-matter potret, Case Pengujian landscape; **jebakan**: reference.docx TAK punya `<w:pgSz>`, jadi tukar-atribut mentah no-op — dipakai API python-docx yang membuat pgSz-nya; lebar kolom dari rasio dash disetel ke proporsi tabel asli supaya tiap header muat tanpa patah mid-kata di landscape 9 inci); 3 field metadata UAT (Quality Review Method, Document Version No/Date); 5 tes penjaga. **Contract B berubah** (`UATTestCase.module`, WAJIB di output LLM demi grouping andal; compiler `.get()` toleran; fixture SDD/UAT + prompt diperbarui — instruksi "nilai module SAMA PERSIS untuk layar sama, urutkan berdampingan"). **Diverifikasi $0** (Contract B sintetis → docx → PDF → PNG → DILIHAT: header hijau, grouping, landscape, langkah multi-baris) LALU **berbayar ~$0,27** (regen UAT MyPertamina dari Contract A cached, Sonnet 5, output 21.061 token): **50 test case, 17 modul unik, 0 tanpa module** — LLM mengelompokkan per-layar dengan benar (CMS: Login/Dashboard/Konten/Harga BBM/Bright Gas/FAQ/Role/User; Mobile: Autentikasi/Data Pengguna; Website: Produk/Cek Harga/Outlet/Promo/FAQ/Sustainability), konten berjejak ke fitur MyPertamina nyata, aktor Admin, langkah bernomor. Visual end-to-end (15 halaman) dilihat: persis bahasa visual PREMCO UAT. **239 test hijau** (+5). Probe: `out/PROBE24_uat_premco_synthetic.docx` (sintetis) & `out/PROBE25_uat_premco_mypertamina.docx` (repo nyata, grouped). MyPertamina jadi repo pertama yang jadi DUA dokumen (SDD premco 2026-07-18 + UAT premco). Batas jujur: format premco UAT dari SATU sumber (PREMCO); V2 (upload template lain) tetap menunggu template dari sumber lain.
- **2026-07-18** — **Deteksi endpoint Next.js App Router: taxonomy 0 → 8 endpoint, dan gap-nya ditemukan DULU oleh tes end-to-end.** Urutan yang benar: tes end-to-end taxonomy mengukur 0 endpoint dari 130 file (bukan ditebak dari membaca kode), itu yang memunculkan gap, baru diperbaiki. App Router tak memakai pemanggilan router (`app.get('/x', ...)`) yang ditangkap `_ts_endpoint_from_call` — handler-nya **named export** `GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS` di file `route.ts`, dan URL-nya dari **struktur folder**, bukan argumen string. **Ditulis dari AST NYATA taxonomy, bukan tebakan grammar** (pelajaran berulang project ini): `export_statement` → field `declaration` (`function_declaration`) → field `name`; `_ts_app_router_endpoints` menangani `export function GET` DAN `export const GET = () =>`. `_app_router_path` menurunkan URL: segmen sesudah `app/`, route group `(grup)` & parallel `@slot` DIBUANG (tak muncul di URL — aturan Next.js), `[id]`/`[...slug]`/`[[...slug]]` → `:id`/`:slug` (memakai mesin kurung yang sama dengan sanitizer PlantUML). Butuh `file_path` (bukan cuma AST), jadi disambung di `parse_file` lalu digabung `endpoints` supaya `_guess_file_type` melihatnya (`route.ts` → controller). Terukur end-to-end: **taxonomy 8 endpoint, semua benar** (`DELETE`/`PATCH /api/posts/:postId`, `GET`/`POST /api/posts`, `PATCH /api/users/:userId`, `GET /api/users/stripe`, `POST /api/webhooks/stripe`, `GET /api/og`). Dijaga 16 test (`tests/test_parser_app_router.py` — file test TS PERTAMA; sebelumnya seluruh jalur TS/JS tanpa test): path derivation (route group, catch-all, `src/app`), fungsi tak-terekspor & `export const dynamic` (config route) diabaikan, `_route.ts` ber-underscore diabaikan, hanya `route.ts` PERSIS yang memicu, Express-style tetap jalan (nol regresi). 234 test hijau (+16). Biaya deteksi: **$0** (deterministik, diukur lewat Tahap 1 gratis). **A/B DIJALANKAN (~$0,20 regen v2): endpoint eksplisit MERAMBAT tapi peningkatannya MARGINAL — persis dugaan.** Bukti merambat: Contract B v2 (Contract A dengan 8 endpoint) memakai notasi parser `:postId`/`:userId` di diagram integrasi, sementara v1 (tanpa endpoint) menebak `{postId}` — jadi endpoint sampai ke LLM dan memengaruhi output. Tapi v1 pun SUDAH akurat (LLM menyimpulkan endpoint yang praktis sama dari nama file), jadi dokumennya sama-sama benar; beda terbesar v1↔v2 adalah **non-determinisme LLM** (aktor Guest/Terdaftar↔Visitor/User, 8↔7 fitur, tata letak diagram), BUKAN endpoint — dan satu run per varian TAK BISA memisahkan keduanya (caveat A/B yang sudah dicatat). Nilai sebenarnya endpoint parser = **akurasi deterministik** (path dijamin benar dari kode, bukan tebakan) yang baru terasa pada repo bernama TAK konvensional; taxonomy kebetulan sangat konvensional sehingga tebakan LLM sudah tepat. Bonus temuan: endpoint auth yang parser TAK deteksi (Pages Router `[...nextauth].ts`) tetap ditebak LLM dgn kurung siku dan **disanitasi** — parser & sanitizer saling melengkapi. Probe: `PROBE22_taxonomy_default_v2.docx`.
- **2026-07-18** — **Tes END-TO-END Next.js (`shadcn-ui/taxonomy`, ~$0,20): fix bracket terbukti pada diagram LLM SUNGGUHAN, bukan sintetis.** Tiga uji bracket sebelumnya ($0) semuanya sintetis — SAYA yang menyusun diagram dari nama file. Uji ini menjalankan pipeline PENUH (ingest → parse → LLM 125 detik → 2 docx) supaya diagramnya digenerate LLM. **Dan LLM secara ALAMI menghasilkan pola bug**: `["POST /api/auth/[...nextauth]"] as AuthEndpoint` — catch-all NextAuth `[...nextauth]` bersarang di dalam label komponen `[...]`, persis kasus yang dulu meng-crash `default`. Sanitizer menanganinya end-to-end → `["POST /api/auth/nextauth"]`, render bersih (nol kurung bersarang). Menariknya LLM memakai `{postId}`/`{userId}` (kurung kurawal, aman PlantUML) untuk KEBANYAKAN param dan hanya catch-all yang pakai kurung siku — fix menangkap satu-satunya yang perlu. **Konten berjejak & akurat**: aktor Guest/Pengguna Terdaftar (bukan Developer), 8 fitur nyata (Auth/Postingan/Billing-Stripe/Blog/OG-Image), system_requirements menyebut Prisma/NextAuth-GitHub-OAuth/Stripe/Contentlayer-MDX/Tailwind — semua benar. Coverage 77% (tinggi — `.tsx` dikenali `ui_component`). Diverifikasi VISUAL: component_integration (template default) render pemetaan FE→API lengkap & bermakna (`UserAuthForm → POST /api/auth/nextauth`, `Editor → PATCH /api/posts/{postId}`, `BillingForm → GET /api/users/stripe`, Stripe → webhook). Efisien: SATU panggilan LLM → Contract B → render DUA template ($0 masing-masing): `PROBE20_taxonomy_default.docx` (580 KB, dgn integrasi) & `PROBE21_taxonomy_premco.docx` (512 KB, gaya Pertamina di Next.js). **Menyingkap keterbatasan parser baru** (App Router route handler tak terdeteksi endpoint — lihat Keterbatasan). taxonomy jadi repo ke-7 yang benar-benar jadi dokumen, TS/Next.js kedua sesudah MyPertamina.
- **2026-07-18** — **Kurung siku route-param Nuxt/Next di PlantUML disanitasi — template `default` tak lagi crash, dan diagram TETAP dirender (bukan placeholder).** Lanjutan langsung dari temuan MyPertamina: nama file dynamic-route `[category]/[slug].vue` masuk ke sintaks komponen PlantUML `[...]`, kurung bersarang merusaknya, dan karena `default` MEMAKAI component_integration, seluruh dokumen gagal (premco sudah kebal sejak melewati diagram itu). Dari tiga opsi yang dicatat (pengerasan prompt / sanitasi / resiliensi placeholder), dipilih **sanitasi** karena: **deterministik** (bukan "biasanya berhasil" ala prompt), **$0 diverifikasi** (script rusak MyPertamina sudah tersimpan, tak perlu regen LLM), dan **mempertahankan diagram utuh** (placeholder membuang isi — padahal component_integration = pemetaan FE↔BE, pembeda utama produk). `_sanitize_route_param_brackets` di `_normalize_plantuml` (tempat compiler memang sudah menormalkan PlantUML dari LLM — filosofi sama dengan reference.docx & preamble gaya): melepas kurung route-param yang TERJEPIT dalam nama file, termasuk catch-all `[...slug]` & optional `[[...all]]`. **Aman dari false-positive lewat DUA syarat**: (1) isi kurung harus identifier murni `[\w-]+` → nama ber-titik `[login.vue]` dan token PlantUML khusus `[*]`/`[H]` TAK tersentuh (isinya bukan identifier murni); (2) kurung harus terjepit char token (`[\w/.]`) → komponen berdiri sendiri `[Login] as X` (dikelilingi spasi/`as`) TAK tersentuh. Diverifikasi berlapis: 8 kasus unit (termasuk yang HARUS diam), end-to-end (default survives, script sanitized sampai ke plantuml.jar), 218 test hijau (+2), dan **VISUAL** — component_integration MyPertamina render benar & lengkap (`product/category/slug.vue → GET /:category/:slug (Product)`, kurung route-param lepas, endpoint `:param` utuh). Probe: `out/PROBE18_mypertamina_default.docx`. Batas diterima: PlantUML rusak lewat jalur LAIN masih gagal-berisik (belum ada resiliensi per-diagram — keputusan sadar). **DIKERASKAN hari yang sama oleh repo Nuxt KEDUA (`nuxt/movies`), yang langsung mengungkap gap overfit**: file yang segmen PERTAMA-nya dynamic route (`pages/[type]/[id].vue`) menghasilkan label komponen diawali `[[type]` (kurung KOMPONEN + kurung param beruntun), dan regex versi pertama (`\[\[?...\]?`) membaca `[[` sebagai optional-catch-all lalu MEMAKAN kurung komponennya → malformed → crash lagi. MyPertamina tak punya pola ini (paramnya selalu di tengah, `product/[category]`), jadi satu repo saja tak cukup membuktikan generalisasi. Diperbaiki dengan mencocokkan kurung BERIMBANG (buka=tutup) lewat dua pola terpisah (double `[[...]]` DULU, lalu single `[...]`). Diverifikasi: 8 nama file ber-kurung nuxt/movies render bersih ke PNG valid (`out/PROBE19_nuxt_other_integration.png`), MyPertamina default tetap OK. **Pelajaran: satu repo membuktikan fix JALAN, repo kedua membuktikan fix GENERAL — overfit hanya kelihatan dari kasus di luar yang menemukannya.** Uji ini $0: tak perlu LLM, cukup tarik nama file ber-kurung asli repo lain lalu render diagram sintetis lewat plantuml.jar. **Repo KETIGA — Next.js `shadcn-ui/taxonomy` (framework & ekstensi beda: `.tsx`, App Router) — lolos TANPA perubahan kode**, membuktikan sanitizer berimbang sudah cukup general: 11 file ber-kurung render bersih termasuk optional catch-all `[[...slug]]`, `[...nextauth]`, dan yang paling menentukan **route group `(marketing)` (kurung BIASA) DIPERTAHANKAN** sementara kurung siku route-param dilepas — dikunci di unit test. Jadi fix terverifikasi lintas 2 framework (Nuxt `.vue` + Next.js `.tsx`) dan seluruh varian kurung (single/catch-all/optional-catch-all, param segmen-pertama, route group).
- **2026-07-18** — **DOKUMEN PREMCO-STYLE PERTAMA DARI REPO TS/JS — klaim "netral-bahasa" terbukti, dan repo NYATA langsung mengungkap bug yang mock lewatkan.** Diuji `MyPertamina.id-Clone` (Vue/JS/TS, ~$0,20, Sonnet 5, LLM 194 detik) — repo yang relevan ke konteks magang pemilik. Sampai hari ini gaya premco cuma diverifikasi pada esteler (Python/Flask); ini membuktikan **pipeline + template premco netral-bahasa** seperti yang dijanjikan arsitektur kontrak. **Kualitasnya berjejak, bukan karangan**: `app_description` benar (harga BBM/Bright Gas, katalog, direktori outlet, promo, FAQ), aktor **Admin/Customer/Pengunjung Publik** (bukan Developer/API Client), 13 fitur memetakan ke CMS & app nyata, dan **tiap system_requirement menyebut buktinya sendiri** ("Nuxt.js — terlihat dari nuxt.config.ts", "JWT melalui utils/jwt.js (signToken, verifyToken)", "Express.js pada backend/src", "userModel/priceModel/roleModel") — Tahap 1 mengukur coverage cuma 27% (85 `.vue` jatuh ke "other") tapi dokumennya tetap berjejak, **menguatkan untuk kesekian kali: label `type` bukan bottleneck**, LLM membaca dari nama fungsi + dependencies. Contract A cuma 36 KB / 18.757 token. **BUG NYATA yang cuma muncul di repo asli** (kelas yang sama dengan 414 mermaid dulu — lolos dari semua test ber-mock): render GAGAL TOTAL karena file dynamic-route Nuxt bernama `[category]/[slug].vue` & `[slug].vue` masuk ke sintaks komponen PlantUML `[...]` → kurung siku bersarang → syntax error. Dan `_build_sdd_context` merender **KEEMPAT** diagram tanpa peduli template, jadi `component_integration` yang rusak — diagram yang **bahkan tak dipakai premco** (V0: premco tak punya bab Integrasi Komponen) — mematikan **SELURUH** generate. **Fix**: `_TEMPLATES_USING_COMPONENT_INTEGRATION = {"default"}` — component_integration hanya dirender kalau template memakainya; premco melewatinya. Diagram yang DIPAKAI tetap gagal-berisik (test membuktikan dua sisi: premco sukses, default tetap `raise DiagramRenderError` — tidak ditelan diam-diam, filosofi utuh). Re-render $0 dari Contract B tersimpan → docx 613 KB, diverifikasi VISUAL (docx→PDF→PNG→dilihat): font Calibri, tabel How to Access/Infrastructure/Remark benar, diagram arsitektur ter-render (User→CMS/Website→Backend API→Basis Data + layanan MyPertamina). 216 test hijau (+1 regresi). Probe: `out/PROBE17_mypertamina_premco.docx`. **Jujur soal batas**: ini konten repo CLONE, bukan kode internal Pertamina — buktinya "format + pipeline jalan di TS/JS", bukan "teruji di produksi Pertamina". Bug default-template pada Nuxt/Next SENGAJA belum diperbaiki (lihat Keterbatasan).
- **2026-07-18** — **Tiga gap `premco`→PREMCO ditutup ($0), dan status "SDD SELESAI" ketahuan distempel untuk template yang SALAH.** Pemilik menegaskan arah: fokus menyempurnakan template Pertamina (magang di sana), SaaS/V2 ditunda karena tidak ada template sumber lain. Pertanyaannya — *"apakah SDD sudah sempurna?"* — dijawab dengan MEMBUKA template, bukan mempercayai catatan: **belum.** "SELESAI secara struktur, bentuk, dan visual" (roadmap tahap a) distempel untuk template `default`, lalu V1 membuat `premco` — template yang justru dimaksudkan JADI dokumen Pertamina — dan status itu **diwarisi tanpa diperiksa ulang**. Kemunculan **KEENAM** pola "memeriksa proksi, bukan barangnya"; kali ini proksinya **catatan status kita sendiri**. Tiga temuan: **(1) FONT — seluruh dokumen ber-Aptos, bukan Calibri acuan.** Sebabnya komentar `build_reference_docx.py` yang MENALAR (*"Calibri kebetulan font tema bawaan Pandoc, jadi tidak perlu diubah"*) padahal bawaan Pandoc adalah **Aptos** — premis salah → kesimpulan "tidak perlu diubah" → font tak pernah diset. Diperparah CLAUDE.md sendiri sudah mencatat *"font Aptos vs Arial"* sebagai perbedaan "SENGAJA" — ada yang MELIHAT Aptos di perbandingan lalu menuliskannya, tapi tak pernah menabrakkannya ke komentar yang mengklaim Calibri; dua catatan bertentangan seminggu tanpa ada yang menyatukan. Kuncinya: **font datang dari TEMA** (`word/theme/theme1.xml` lewat `asciiTheme=minor/majorHAnsi`), BUKAN style — itu sebabnya SEMUA style benar tapi tiap huruf salah, dan tak satu pun test lama bisa menangkapnya. `_set_theme_fonts` menulis ulang `<a:latin>` major+minor jadi Calibri (diukur dari docDefaults acuan yang menyetel Calibri eksplisit, bukan ditebak); test penjaga baru memeriksa TEMA dokumen hasil. Menyentuh **KEDUA** template karena reference.docx dipakai bersama — artinya semua dokumen seminggu terakhir keluar ber-Aptos. **(2) How to Access → tabel checklist 2 baris** (Internal, Published to Internet; kolom Deskripsi YES/NO + Remark) — diukur dari docx asli (`measure_bab.py`), dulu satu field teks bebas `how_to_access` yang tak mungkin menyerupai tabelnya. Field diganti 4 field `access_*` di schema; YES/NO jadi dropdown; berlaku di kedua template. **(3) Infrastructure & Capacity (premco) → kerangka 22 baris kosong** (Akses URL/Server/DB/Web Service per environment, Rev. Proxy, Team Foundation Server) diisi manual di Word — isinya URL deployment, bukan turunan kode; template `default` tetap teks bebas. Penomoran tabel bergeser (How to Access = Tabel 4, sisanya +1) di kedua template; test penomoran menangkapnya. **Koreksi rencana yang dibayar pengukuran, bukan selera:** "Deskripsi Aplikasi → list fitur utama" SEMULA dikira $0, ternyata butuh Contract B (bab itu memuat subset fitur TERKURASI "tiga utama" yang tak ada di `feature_requirements` yang datar) — pindah ke antrian regen ~$0,25 bersama SysReq 2-tabel & flow bersarang. **Jebakan Word COM baru** (untuk siapa pun yang mengotomatiskan verifikasi visual lagi): `updateFields=true` di reference.docx memicu dialog "update fields?" saat file dibuka; dengan `Visible=False` dialog tak terlihat dan Word **menggantung selamanya** (CPU mentok lalu datar — tanda modal, bukan komputasi), dan `DisplayAlerts=0` TIDAK menjangkaunya. Solusi: buang `<w:updateFields>` dari SALINAN docx sebelum dibuka, TOC diisi manual lewat COM `TablesOfContents.Update()`. Jebakan kedua: NBSP — Pandoc smart-punctuation menukar spasi sesudah "Rev." jadi `\xa0` ("Internal Rev.\xa0Proxy"), assertion `in` gagal walau di layar identik; `_docx_text` menormalkannya. 215 test hijau (212+3: font tema, tabel How to Access, kerangka Infrastructure). Diverifikasi VISUAL (render ulang Contract B esteler → docx → PDF via Word COM → PNG per halaman → DILIHAT): font Calibri, kedua tabel benar disandingkan acuan. Probe: `out/PROBE16_font_tabel_premco_esteler.docx`. **Yang MASIH menggantung**: perbandingan berdampingan 16 bab `premco`×PREMCO belum dilakukan (bisa memunculkan gap ketujuh); regen ~$0,25 untuk 3 gap Contract B; UAT premco (butuh acuan UAT Pertamina — TIDAK ADA di repo, blocker nyata tahap b).
- **2026-07-17** — **V1 MULTI-TEMPLATE: template kedua "premco" (kompilasi manual dari docx asli) + `template_id` di form — pipeline resmi melayani lebih dari satu gaya dokumen.** Registry `_TEMPLATE_REGISTRY` di compiler (`default`, `premco`; premco baru menyediakan SDD), dipilih user lewat `template_id` di `GenerateDocumentRequest` + dropdown "Gaya Dokumen" di frontend; kombinasi yang tidak tersedia ditolak **422 SINKRON** lewat `validate_template` — sebelum job dibuat, sebelum LLM dibayar. Isi dokumen (Contract B) tidak berubah sama sekali — yang berbeda murni penyajian, dan itulah bukti arsitektur kontraknya: template premco = konvensi TERUKUR dokumen aslinya (V0): bab tanpa nomor, TANPA Component Integration (aslinya memang satu gambar arsitektur; aturan V1 "mengisi, bukan merestrukturisasi"), tabel Features ber-kolom Remark, DUA bab mockup, dan yang paling khas **tabel use case/activity ber-BAR JUDUL biru** (`9CC3E5`, warna terukur dari 27 sel dokumen asli) dengan acceptance criteria & langkah activity DI DALAM sel. Dua mekanisme marker baru di post-process — marker-driven supaya template default tak tersentuh: (1) `((BAR))` → `_apply_title_bars`: merge baris pertama + biru + bold + matikan header-hitam kondisional per-tabel (`tblLook firstRow=0` — teks putih di atas biru muda tak terbaca) + ikatan keepNext supaya tabel utuh se-halaman (versi pertama patah tepat sesudah bar, dengan bar terulang sebagai tblHeader — tertangkap mata di probe); (2) `((BR))` → `_expand_line_break_markers`: **`<br/>` DIBUANG TANPA SUARA oleh writer docx Pandoc** (raw HTML tidak didukung; langkah activity menyambung jadi "login.2. Admin..." — lagi-lagi hanya kelihatan dari melihat halaman), dan sel ber-break dipaksa rata KIRI karena justify + line break manual = tiap baris direntangkan Word sampai jelek. Diverifikasi berdampingan dengan halaman use case & activity dokumen ASLI: konvensi penyajiannya sama. 212 test hijau (205+7). Biaya **$0**. Probe: `out/PROBE15_template_premco_esteler.docx`. Catatan proses yang memalukan tapi jujur: logo uji di `frontend/dist/assets` TERHAPUS oleh `npm run build` yang dijalankan sesi ini sendiri — persis jebakan yang baru diperingatkan; diselamatkan dari media PROBE13. Jangan simpan aset di `dist/`.
- **2026-07-17** — **V0 "upload template perusahaan" (roadmap tahap c) dikerjakan pada template NYATA — dua koreksi arah dibayar $0.** Pemilik menyediakan docx PREMCO asli (40,7 MB di root; ter-gitignore otomatis lewat pola `*.docx` — dokumen internal klien). Tiga pertanyaan V0, tiga jawaban: (1) **Pemetaan bab→Contract B: 16/16 bab terpetakan tanpa ambigu di level isi**; ambiguitas hanya di level FORMAT (System Requirement mereka DUA tabel vs list kita; langkah flow BERSARANG vs flat; Infrastructure tabel 23 baris vs teks bebas form; `component_integration` tidak punya rumah). Unit activity diagram mereka berulang regular sempurna (5 elemen × ±20) — deteksi blok berulang punya pola untuk dipegang. (2) **Swap `--reference-doc` mentah = jalan buntu TOTAL, bukan parsial** (diprobe sungguhan): Pandoc memakai reference-doc sebagai KONTAINER paket, jadi media+font ter-embed dokumen user ikut terseret (output 40 MB), python-docx menolak paketnya, dan **Word sendiri menyatakan corrupt**; lagipula dari 14 nama style yang dicari Pandoc, docx mereka cuma punya 4. Jalur yang benar: **SINTESIS** — langkah kompilasi template MENGUKUR properti visual dokumen user lalu MEMBANGUN reference.docx bersih; terbukti terukur mekanis (Calibri 11pt, H1 14pt center bold, header tabel hitam, bar judul use case biru `9CC3E5`) dan angkanya PERSIS yang di-hardcode manual ke `build_reference_docx.py` minggu ini — pendekatan sintesis sudah pernah terjadi sekali secara manual, V2 tinggal mengotomatiskan pengukurannya. (3) **Template nyata datang dengan pembusukannya sendiri** (6 heading kosong, nomor gambar kacau/dobel, caption tanpa nomor — kelas CORETAD): langkah kompilasi wajib membersihkan, bukan menyalin buta. Laporan lengkap + peta bab: `scripts/validation/V0_TEMPLATE_PREMCO.md`. Rekomendasi V1: kompilasi MANUAL template PREMCO jadi template kedua di sistem + pipeline belajar melayani >1 template. Batas jujur V0: SATU template, dan dari perusahaan yang sama dengan acuan kita — generalisasi butuh 1-2 template sumber lain.
- **2026-07-17** — **Slot upload logo di form: perbedaan terakhir yang bisa ditutup dari perbandingan PREMCO, tertutup.** Logo (PNG/JPEG ≤ 2 MB) masuk sebagai `logo_base64` di `GenerateDocumentRequest` dan ditanam di **kanan-atas header TIAP halaman** lewat post-process (`_add_header_logo`) — reference.docx itu statis dibangun sekali, sementara logo datang per-request, jadi satu-satunya titik temu adalah sesudah docx jadi. **Validasi SINKRON saat POST** (`decode_logo`: base64 → batas 2 MB → Pillow `verify()`): file rusak ditolak **422 sebelum job dibuat**, jauh sebelum LLM dibayar — prinsip yang sama dengan guard context window; dan yang memvalidasi isi gambarnya Pillow, bukan tebakan mime dari klien (pelajaran lama: header JPEG dibaca sebagai PNG memberi angka ngawur tanpa error). Ukuran tampil pakai aturan `_image_attr`: logo normal dibatasi TINGGI 0,45", logo pita >5,3:1 dibatasi LEBAR 2,4" supaya tidak menabrak area teks. Frontend: input file di panel Dokumen, dikirim sebagai data-URL apa adanya (backend yang membuang prefiks `data:image/...;base64,` — frontend tidak wajib bersih-bersih). Kosong = dokumen persis seperti sebelum fitur ini ada. **Tepi transparan logo DIPANGKAS sebelum ukurannya dihitung** (`_trim_transparent_edges`) — ketahuan dari logo uji pertama yang dikirim pemilik project (logo Pertamina 964×288 di kanvas 1024×576 ber-padding transparan 144px atas-bawah): tanpa pemangkasan, "setinggi 0,45 inci" yang KELIHATAN cuma 0,22 inci. Hanya alpha yang dipangkas — memangkas "tepi putih" JPEG itu menebak (putih bisa bagian logo), sedangkan piksel alpha-nol memang dinyatakan kosong oleh file-nya sendiri. 205 test hijau (198+7). Verifikasi visual $0 dua kali: PROBE12 (logo placeholder PIL), lalu **PROBE13 dengan logo Pertamina sungguhan disandingkan dengan PREMCO** — ukuran & posisi logo di header praktis identik dengan acuan, di cover maupun halaman isi.
- **2026-07-17** — **REDESIGN VISUAL DOKUMEN ke bahasa visual PDF acuan — dan untuk pertama kalinya halaman acuan DILIHAT SEBAGAI GAMBAR, bukan dibaca teksnya.** Permintaan pemilik project: "kualitas visual harus setara PREMCO saat disandingkan; JANGAN sentuh pipeline/kontrak" — dan memang nol perubahan Contract A/B/prompt/LLM. Melihat halamannya (bukan teksnya) langsung mengungkap grammar visual yang tidak mungkin ketahuan dari teks: heading bab DI TENGAH bold caps (bukan kiri gaya Markdown), body justified, header tabel hitam-teks-putih-di-tengah, footer = judul miring kiri + nomor kanan, Daftar Isi ber-dot-leader, dan **urutan halaman**: cover+identitas → revisi → persetujuan → daftar-daftar → isi. Lima perubahan, semuanya lapisan presentasi: (1) **reference.docx dirombak** (Title 22pt, H2 14pt centered caps + letterspacing, body justified line 16, style `toc 1..3`/`table of figures` ber-dot-leader — TIDAK ada di kerangka Pandoc, tanpanya rupa daftar tergantung versi Word pembaca; TOC Heading ber-pageBreakBefore; tabel: padding 80/115 twip, trHeight min, vAlign center, cantSplit). (2) **Urutan halaman acuan tercapai dengan MELEPAS --toc/--lof/--lot untuk SDD**: Pandoc memaku ketiga daftar tepat sesudah judul (itu sebabnya Daftar Isi selama ini nongkrong di halaman cover); field code yang SAMA PERSIS kini ditanam `sdd_template.md` sesudah Persetujuan, dan tetap terisi karena `updateFields` dibawa settings.xml reference.docx — **diprobe dulu sebelum dipakai**: Pandoc menyalin settings.xml dari reference doc. UAT tetap `--toc` (tahap b roadmap, belum disentuh). (3) **Lebar kolom = rasio dash di separator template** (`--columns=20` menurunkan ambangnya) — tanpa ini tabel pendek jatuh ke autofit Word dan kolom kosong (Nama di Tim & Peran) menyusut nyaris nol. (4) **Diagram**: dpi 200→300, `-DPLANTUML_LIMIT_SIZE=16384` (PlantUML diam-diam MEMOTONG render >4096px — pada 300 dpi activity diagram panjang pasti kena, tanpa error), dan `_image_attr` berhenti meng-upscale: ukuran tampil = piksel/360, diagram kecil yang direntangkan selebar halaman itu buram + hurufnya raksasa + ukuran teks antar diagram jadi acak. (5) **Post-process python-docx di compiler** (`_postprocess_docx`): header tabel dirata-tengah — jalan memutar yang terpaksa: Word MENGABAIKAN `w:pPr` (jc) dari `tblStylePr firstRow`, diprobe dengan compat flag `overrideTableStyleFontSizeAndJustification` true/false/absen, ketiganya identik; `rPr`/`tcPr` dihormati, `pPr` tidak. Ditambah (permintaan pemilik, hari yang sama): **caption tabel dipindah ke BAWAH tabelnya** seperti acuan — Pandoc selalu menulisnya di atas tanpa tombol pemindah; caption dijaga tidak yatim di batas halaman lewat keepNext pada baris terakhir tabel (bukan pada caption — arah ikatannya terbalik sejak caption di bawah). Lalu dari **perbandingan berdampingan 12 pasang halaman PREMCO×esteler** (dikerjakan sendiri, komposit kiri-kanan per bagian): satu-satunya gap fungsional tersisa = **baris tanda tangan setinggi teks biasa** (acuan memberi kotak ±4 cm untuk tanda tangan basah) → baris kosong tabel ber-kolom "Tanda Tangan" kini ≥1 inci, dan bloknya (label + header + baris) diikat keepNext supaya utuh pindah halaman — versi pertamanya patah tepat sesudah header, ketahuan juga dari melihat. Yang tinggal beda dengan acuan dan SENGAJA: logo header tiap halaman (tidak punya asetnya — kandidat slot upload), ~~font Aptos vs Arial~~ **[KOREKSI 2026-07-18: ini BUKAN "sengaja", ini BUG — acuannya Calibri, bukan Arial, dan reference.docx keluar ber-Aptos karena font tak pernah diset; sudah diperbaiki, lihat Riwayat 2026-07-18]**, acceptance criteria di luar tabel. Perbandingan itu juga mempertontonkan klaim jualan lama secara harfiah: **Daftar Gambar PREMCO seluruh entrinya menyebut aplikasi lain (CORETAD), punya kita benar semua ber-dot-leader** — halaman rusaknya acuan kini ada tangkapannya berdampingan dengan halaman kita yang benar. Dua jebakan Word lain yang tertangkap di putaran verifikasi: **fldSimple membuang format run saat Word meng-update field** (footer sempat balik ke 12pt tegak → diganti field kompleks fldChar dengan rPr di run kode — cara Word sendiri menyimpan field), dan **titik dua kiri di separator pipe table** (`|:---|`) menyuntik `w:jc="left"` LANGSUNG ke tiap sel, mengalahkan semua style. Diverifikasi 4 putaran dengan MELIHAT, bukan membaca XML: render ulang dari Contract B esteler tersimpan ($0) → docx → PDF lewat Word COM sungguhan (sekalian membuktikan ketiga daftar terisi TANPA refresh manual) → tiap halaman di-render PNG dan diperiksa mata. 196 test hijau (191+5: no-upscale, header center, dot-leader styles, updateFields, urutan daftar-sesudah-persetujuan). Biaya: **$0**. Probe untuk dibaca pemilik: `scripts/validation/out/PROBE9_visual_premco_esteler.docx` (675 KB — naik dari 432 KB, harga dpi 300).
- **2026-07-16 (sesi baru)** — **MIGRASI DIAGRAM: Mermaid/mermaid.ink → PlantUML LOKAL, digabung pendalaman Contract B ke bentuk acuan (Kelas B).** Keputusan pemilik project setelah melihat lembar perbandingan berdampingan (`scripts/diagram_comparison.py`). Satu perubahan, empat hal sekaligus: (1) **renderer**: `_run_plantuml` menjalankan `plantuml.jar -pipe -tpng -Playout=smetana` lokal — isi diagram berhenti keluar ke internet (keterbatasan privasi mermaid.ink TUTUP), batas URL 8KB hilang, larangan paralelisasi 503 tidak berlaku lagi; gaya visual (`!theme plain` + `skinparam dpi 200`) disuntik compiler lewat `_normalize_plantuml`, LLM dilarang menulis theme/skinparam — filosofi reference.docx untuk diagram. Dependency baru: **Java 17+ & plantuml.jar** (gagal-berisik dengan pesan cara pasang). **Jebakan yang dikunci test**: PlantUML MENGGAMBAR syntax error sebagai PNG dengan exit code non-nol — kalau cuma percaya stdout, gambar error ter-embed diam-diam sebagai "diagram"; periksa exit code DAN magic PNG. (2) **Contract B berubah besar** (lihat bagian Kontrak): `mermaid_script`→`diagram_script` (PlantUML), activity diagram membawa `actor`/`pre_condition`/`steps[]`, plus `user_roles[]`, `business_flow_steps[]`, `diagrams.business_process_flow`, dan `system_requirements` jadi terstruktur `{name, detail}` — semuanya bentuk yang dituntut dokumen acuan (tabel ACT001 per activity, tabel role, tahapan flow bernomor, tabel system requirement). Contract B lama TIDAK kompatibel. (3) **Template**: tabel metadata per activity (No. `ACT%03d` ditanam deterministik dari urutan, bukan diminta ke LLM), tabel role = Tabel 1 (persis acuan), penomoran gambar/tabel digeser (4 gambar tetap, 5 tabel tetap; tabel activity melanjutkan dari use case — offset memuat `use_cases|length`). (4) **Prompt**: "ATURAN BUKTI UNTUK DIAGRAM" eksplisit (perbaikan yang ditunggu sejak temuan fastapi — efek belum terverifikasi), aturan PlantUML per jenis diagram dengan contoh. Diverifikasi: 191 test hijau (mock pindah ke `_run_plantuml`, proses eksternal saja — normalisasi & penulisan file tetap teruji asli), smoke render NYATA dari fixture (5 PNG UML valid ter-embed, script rusak ditolak dengan stderr PlantUML asli — perilaku exit code diverifikasi empiris, bukan diasumsikan). **Regen esteler BERHASIL end-to-end** (~$0,25-0,30, LLM 164 detik, docx 432 KB): 11 fitur, 8 use case (Admin & Customer), **8 activity diagram semuanya ber-`actor`/`pre_condition`/`steps` dan ACT001-ACT008 terisi benar di tabel**, 2 role, 9 system requirement terstruktur (semua berjejak — bahkan menyebut buktinya sendiri: *"PostgreSQL, di-hosting di Neon (terdeteksi dari normalisasi...)"*), 8 tahapan flow bisnis; **seluruh 12 diagram PlantUML valid** (semua `@startuml`, nol `!theme`/`skinparam` — larangan dipatuhi), use case diagram ter-render sebagai UML sungguhan (stick-figure + kotak "Sistem Esteler" + 13 oval), dan **kelima komponen arsitektur berjejak di Contract A** (User/Flask/PostgreSQL-Neon/Cloudinary/Groq). Penomoran ujung-ke-ujung benar: Gambar 1-12, Tabel 1-21. **Bonus temuan dari kejelian pemilik project** ("kredit tidak berkurang, aneh?"): regen pertama ternyata TIDAK PERNAH memanggil API — harness punya konfirmasi interaktif `Lanjut? [y/N]` sebelum panggilan berbayar, dan di shell non-interaktif `input()` menunggu selamanya (proses "jalan" 30+ menit = nol koneksi, CPU 1,8 detik, $0). Diperbaiki dengan flag `--yes`. Pelajarannya: proses yang kelihatan lambat belum tentu bekerja — cek koneksi jaringan & CPU-nya, bukan cuma statusnya; dan sinyal biaya (kredit tidak bergerak) adalah alat diagnosis yang sah.
- **2026-07-16 (sesi baru)** — **PDF acuan akhirnya DIBACA UTUH 87 halaman dan dibandingkan bagian-per-bagian dengan dokumen esteler jadi — dan perbandingan itu langsung menangkap satu bug nyata.** (1) **Bug: heading use case 11.2–11.8 bocor jadi teks literal.** Pandoc mensyaratkan baris kosong SEBELUM heading (`blank_before_header`) — kembaran persis bug Acceptance Criteria, di sisi sebaliknya. `trim_blocks` membuat `### 11.2 ...` menempel di baris kriteria terakhir → keluar sebagai teks `### ...` di dalam list item: bukan heading, hilang dari Daftar Isi, tanpa error. **Lolos dari 186 test karena fixture cuma punya 1 use case, dan bug-nya baru muncul mulai use case KEDUA** — pelajaran baru: fixture minimal (N=1) membutakan test terhadap seluruh kelas bug *antar-item*. Test baru menumbuhkan fixture jadi 2, terbukti gagal di template lama, dan diverifikasi ke dokumen esteler nyata via render ulang dari Contract B tersimpan ($0): 8/8 heading jadi heading sungguhan. 187 test hijau. (2) **Analisis gap ke acuan** (hasil lengkap di SESSION.md sesi ini): struktur bab memang sudah setara, tapi **bentuk penyajian per-bagian masih beda** — terbesar: acuan memberi tiap activity diagram TABEL metadata (No. ACT001, Actor, System, Pre-Condition, Description berlangkah-bernomor) sementara kita cuma nama + 1 kalimat; lalu tabel role pengguna di Deskripsi Aplikasi, System Requirement berbentuk tabel terstruktur (bukan bullet), Flow Proses Bisnis bertahapan-bernomor + diagram (bukan 1 paragraf), kerangka Timeline/Cost/tanda-tangan di Persetujuan, dan section Mockup sebagai placeholder. Perbaikannya dua kelas: template-saja ($0, verifikasi render-ulang) vs perlu perubahan Contract B + prompt (1 regen esteler ~$0,24 memverifikasi semuanya sekaligus). **Kelas template-saja SELESAI di sesi yang sama**: halaman muka (Fungsi/Kodifikasi, Katalog Proses Bisnis, Tim & Peran), kerangka Timeline + Cost Estimation + dua blok tanda tangan di Persetujuan, dan bab "13. Mockup Antarmuka" (satu bab, bukan dua seperti acuan — template generik tidak tahu platform aplikasi; sel kerangka sengaja KOSONG, bukan `(diisi manual)`, karena tidak dipetakan ke form). 189 test hijau. (3) **Lembar perbandingan bahasa diagram dibuat** (`scripts/diagram_comparison.py`, $0): use case + activity + arsitektur esteler dari Contract B tersimpan dirender Mermaid vs PlantUML vs D2 berdampingan di satu HTML. PlantUML & D2 dirender **LOKAL** (plantuml.jar + d2.exe, layout smetana) — rencana kroki.io ditolak guard keamanan, dan penolakan itu benar: isi diagram aplikasi anggota tim tidak boleh dikirim ke layanan pihak ketiga baru secara sepihak. Temuan yang sudah terlihat sebelum manusia menilai: use case Mermaid esteler berukuran 1600×4270 (kurus-menjulang, di dokumen menyusut karena dibatasi tinggi halaman) sementara PlantUML kompak dan persis gaya UML acuan (stick-figure + oval + kotak sistem).
- **2026-07-16** — **Kualitas visual: tiga cacat yang terlihat kosmetik ternyata merusak fungsi.** Diminta pemilik project ("output harus terlihat profesional seperti PDF acuan") — dan instingnya menemukan apa yang seharian luput, karena **seluruh pemeriksaan sebelumnya menguji STRUKTUR dan ISI, tidak pernah TAMPILAN**. Pipeline/Contract A/Contract B tidak disentuh; semua perubahan di `reference.docx` + template. **(1) "Diagram blur" ternyata JPEG, bukan resolusi.** Endpoint `/img/` mermaid.ink membalas **JPEG** secara default — format lossy untuk foto, dipakai pada *line art* dia menaruh artefak di sekeliling tiap garis dan huruf. Filenya bahkan disimpan berakhiran `.png` padahal isinya JPEG, yang menyembunyikan masalahnya dari siapa pun yang cuma melihat nama file. Ditambah 77 dpi (butuh ≥150 untuk cetak). Sekarang `type=png&width=1600` → PNG lossless, 246 dpi. **(2) Acceptance Criteria dilebur jadi satu paragraf gembung.** Markdown mensyaratkan list didahului **baris kosong**; tanpa itu `1.` dianggap lanjutan paragraf `Acceptance Criteria:`, jadi 8 use case × beberapa kriteria semuanya jadi blok teks tak terbaca. Terukur: paragraf `Compact` **11 → 42** — 31 item yang sebelumnya terlebur. **(3) Lima dari sebelas gambar tumpah keluar halaman** (setinggi 17,2 inci di halaman 11 inci), karena semua gambar direntangkan selebar halaman padahal activity diagram tinggi-sempit. `_image_attr` kini membatasi sisi yang lebih dulu mentok → **11/11 muat**. **Ketiganya gagal SUNYI**: dokumen tetap jadi, tetap lengkap, tetap lolos setiap test — cuma jelek atau tak terbaca. Tampilan: Heading 2 (bab) 15pt **bold huruf besar** hitam + napas (bawaan Pandoc: biru muda tanpa bold — terbaca seperti render Markdown); tabel header **hitam teks putih**, border 6 sisi, padding atas/bawah (bawaan Pandoc **0** — itu penyebab baris mampet); caption 9pt italic center. **Header tabel dipasang lewat `tblStylePr firstRow`, BUKAN memformat sel** — Pandoc menghasilkan sel `<w:tcPr/>` KOSONG dan menyalakan `<w:tblLook w:firstRow="1">`, jadi Word sendiri yang menerapkan format baris pertama dari style; artinya seluruh rupa tabel diatur dari satu tempat dan template tidak perlu tahu apa-apa soal warna. Ukuran font **diukur dari PDF acuan** (badan ~13, heading ~19, rasio ~1,45×, font Calibri — yang kebetulan sudah jadi font tema bawaan Pandoc), bukan ditebak. `Pillow` dinyatakan eksplisit di `requirements.txt`: sebelumnya cuma nebeng lewat `python-docx` (yang cuma dipakai `scripts/` & `tests/`, bukan `app/`) — dan godaan parse header PNG manual ditolak karena itu **persis yang menipu di sesi ini**: header JPEG dibaca sebagai PNG memberi `65536 × 4293001688` tanpa error. **Font/warna di luar itu sengaja tidak disentuh**: rupa PDF acuan tidak bisa dilihat dari sini (cuma teksnya yang terbaca), dan menebak estetika yang tak bisa diverifikasi bukan pekerjaan yang jujur. Diverifikasi manusia: *"tampilannya jauh lebih rapi sekarang."* 183 test hijau (174 + 9). Biaya: **$0**.
- **2026-07-16** — **Daftar Gambar & Daftar Tabel: celah struktur terakhir yang bisa diselesaikan, tertutup — dan Word yang menghitung nomor halamannya, bukan kita.** Dokumen kita sudah punya gambar (SDD esteler: 11) dan tabel (11), tapi tidak bernomor, tanpa caption, tanpa halaman indeks. Yang tampak jadi penghalang: nomor halaman cuma ada setelah Word merender, sementara kita bekerja di Markdown. **Ternyata itu bukan urusan kita sama sekali** — `--lof`/`--lot` membuat Pandoc menulis **FIELD CODE Word** (`TOC \h \z \t "Image Caption" \c`), dan Word menghitung paginasinya sendiri saat dokumen dibuka. **Diverifikasi dengan MEMBUKA docx-nya di Word** (11 baris + nomor halaman, tanpa refresh manual) — membaca XML tidak akan pernah membuktikan itu, dan probe-nya gratis karena Contract B esteler sudah tersimpan (nol panggilan LLM). Template sudah menulis `![Arsitektur Sistem](...)`, yang Pandoc ubah jadi caption bergaya `Image Caption` — jadi separuh mekanismenya sudah terpasang tanpa disengaja. **Judul Indonesia butuh DUA mekanisme Pandoc yang berbeda**, dan itu ditemukan dengan mencoba, bukan diasumsikan: `lang=id` menerjemahkan "Daftar Gambar"/"Daftar Tabel" (`lof-title`/`lot-title` **diabaikan** writer docx), sementara "Daftar Isi" justru **tidak** ikut `lang` dan cuma bisa lewat `toc-title`. **Penomoran ditanam di teks caption** (`Gambar {{ loop.index + 3 }} ...`) karena Pandoc tidak menomori sendiri; offset `+3` itu kopling rapuh — tambah satu gambar tetap tanpa menaikkannya dan nomornya bentrok **tanpa error apa pun**, jadi ada test yang menumbuhkan loop-nya dan mengunci urutan `1..6`, plus komentar kontrak di kepala template. Tabel front-matter sengaja tidak di-caption: **acuan pun tidak menomorinya**, penomoran isi mulai dari Informasi Demografi. **Satu bug tertangkap sebelum commit**: `--lof/--lot` sempat dipasang global, dan UAT (nol gambar, satu tabel isi) langsung dapat **dua halaman indeks kosong** — persis kegagalan yang diperingatkan sendiri di sesi ini ("halaman hampa lebih buruk daripada tidak ada"). Sekarang SDD-saja, dengan test penjaganya. Dua kesalahan sendiri yang layak diingat: `'TOC \h \z \t ...'` — `\h`/`\z`/`\c` kebetulan bukan escape Python jadi lolos, tapi **`\t` adalah TAB**, satu dari empat backslash berubah diam-diam (r-string memperbaikinya); dan assertion `"List of Figures" not in xml` **gagal** karena string itu memang ada sebagai `<w:docPartGallery>` — identifier internal Word yang tidak pernah dilihat pembaca. **Menguji XML mentah = menguji proksi**; test-nya sekarang mengekstrak isi `<w:t>`. 156 test hijau (149 + 7). Biaya: **$0**.
- **2026-07-16** — **`url_prefix` Blueprint Flask: dua controller berhenti mengklaim endpoint yang sama — dan diagnosis yang tercatat setahun ini ternyata salah.** Gejalanya: `routes/admin.py` dan `routes/customer.py` esteler sama-sama melaporkan `GET /` **dan** `GET /menu` (dua bentrokan, bukan satu seperti tercatat), jadi fitur admin dan fitur pelanggan tampak berbagi endpoint di dokumen yang sudah dikirim. **CLAUDE.md mendiagnosisnya sebagai masalah lintas-file** — *"blueprint biasanya didaftarkan di file lain (`app.register_blueprint(auth_bp, url_prefix="/auth")`)... butuh analisis lintas-file"*. **Kode esteler tidak begitu**: prefix-nya ada di konstruktor, `admin_bp = Blueprint("admin", __name__, url_prefix="/admin")`, **di file yang sama dengan route-nya**, dan `register_blueprint`-nya dipanggil **tanpa** `url_prefix` sama sekali. Diagnosis lama ditulis dengan mencocokkan idiom Flask yang lazim, bukan dengan membaca repo-nya — **pola "memeriksa proksi, bukan barangnya" untuk kelima kalinya**, dan kali ini proksinya adalah pengetahuan umum tentang framework. Akibatnya bukan cuma salah: dia membuat perbaikan ini tampak jauh lebih mahal daripada aslinya, dan menahannya berbulan-bulan. **Hasil terukur pada esteler: 18 dari 38 endpoint jadi `/admin/...`, bentrokan 2 → 0, total endpoint tetap 38** (tidak ada yang hilang atau muncul — cuma path-nya benar). Nol regresi ke repo nyata: fastapi 49 file/0 endpoint, flask 25/0, express 8/33, realworld 69/27 — semua identik. Tiga keputusan yang layak diingat: (1) **pengumpulan prefix jadi PRA-PASS tersendiri** — Flask tidak mewajibkan Blueprint dibuat sebelum route-nya ditulis (pola factory menaruhnya di dalam fungsi), jadi menyandarkan urutan pada urutan file itu rapuh; (2) **gagal-membuka** — objek yang bukan Blueprint dikenal (`app`, `router` FastAPI) dapat prefix `""`, karena menebak prefix yang salah menaruh endpoint di URL yang **tidak ada**, dan itu lebih buruk daripada path kurang lengkap; (3) `_java_join_path` ternyata **sudah** menangani keenam kasus Flask dengan benar (termasuk `/admin` + `/` = `/admin/`) — jadi dia dipindah jadi `_join_url_path` yang dipakai bersama, bukan disalin. Dua tempat menyambung path adalah dua jawaban yang menunggu untuk berbeda. Bonus: bug laten ketemu sambil membaca — `endpoints.append(found)` seharusnya `extend` (cabangnya tidak pernah tercapai, jadi tidak pernah menggigit). 149 test hijau (138 + 11). Biaya: **$0**.
- **2026-07-16** — **Parser Java + Spring MVC: bahasa enterprise pertama yang didukung, dan prioritas yang menahannya ternyata mengukur benda yang salah.** Sebelumnya SELURUH file `.java` jatuh ke entry `type: "other"` kosong — `.java` ada di `SOURCE_EXTENSIONS` (jadi filenya ter-ingest) tapi tidak di `EXTENSION_TO_LANGUAGE`. Terukur pada spring-petclinic: **33 file → 0 class, 0 dependency, 0 endpoint**. Sekarang: **25 class, 17 endpoint, coverage 0% → 33%**, dan tipe otomatis terisi (`controller` 6, `repository` 3, `model` 2) **tanpa menyentuh heuristik `type` sama sekali** — Java memang gaya penamaan `XxxController` yang heuristik itu dirancang untuknya; yang selama ini menghalangi cuma `build_parsed_repo_context` yang meng-hardcode `type="other"` di jalur fallback dan tidak pernah memanggil `_guess_file_type`. **Ini membatalkan prioritas #1 yang tercatat di SESSION.md**: rencananya *"ukur Contract A petclinic dalam token dulu, gratis, hasilnya menentukan prioritas"* — tapi mengukurnya sebelum parser Java ada berarti mengukur **daftar path kosong** (6,4 KB), bukan Contract A Java. Angkanya bahkan menyesatkan ke arah optimis ("Java cuma 6 KB, muat lega") justru karena kosong; dan yang mahal di Contract A — `classes` 21%, `dependencies` 23% — persis field yang nol di situ. Urutannya terbalik secara logis: **ukuran Contract A adalah konsekuensi dukungan Java, bukan prasyaratnya.** Ini kemunculan **keempat** pola "memeriksa proksi, bukan barangnya", dan kali ini proksinya ada di rencana kerjanya sendiri. Tiga jebakan grammar yang semuanya gagal SUNYI (nol endpoint tanpa error) kalau pola Python disalin apa adanya: (1) **`modifiers` BUKAN field** — `child_by_field_name("modifiers")` selalu None, beda dari `name`/`body`/`parameters`; (2) `_string_literal_value` menolak Java karena mengecek `type != "string"` sementara Java memakai `string_literal`; (3) **Javadoc adalah `block_comment` sibling sebelum deklarasi**, bukan di dalam body seperti docstring Python. `interface_declaration` ditangani sejajar dengan class karena **Spring Data menulis seluruh repository sebagai interface** — menangani `class` saja membuat lapisan data hilang total. **Satu bug lolos dari angka dan cuma ketahuan karena path-nya diperiksa satu per satu**: `@GetMapping({"/vets"})` (array posisional, dipakai VetController) tidak cocok cabang mana pun → path jatuh ke None → keluar sebagai `/`, sehingga VetController dan WelcomeController sama-sama melaporkan `GET /` — **gejala yang sama persis dengan url_prefix Blueprint Flask**. "17 endpoint, naik dari 0" terlihat seperti sukses total; satu di antaranya diam-diam salah. Bonus: `@RequestMapping("/owners")` di level class disambung ke method (`/owners/{ownerId}` + `/pets/new` = `/owners/{ownerId}/pets/new`) — masalah yang sama dengan `url_prefix` Flask, tapi di Java kedua bagiannya ada di **file yang sama** jadi bisa deterministik tanpa analisis lintas-file. Nol regresi dibuktikan ke repo nyata (bukan cuma mock, karena `_string_literal_value` dipakai bersama): fastapi 49 file, flask 25 file/52 class/72 function/154,9 KB, express 8 file, realworld 69 file/27 endpoint — semua identik. 138 test hijau (119 + 19). Biaya parser: **$0**.
- **2026-07-16** — **DOKUMEN JAVA PERTAMA, dan seluruh isinya berjejak.** Tahap 2 dijalankan ke `spring-petclinic` (~$0,23, Sonnet 5, 128 detik): 5 fitur, 5 use case, 5 test case, 5 activity diagram, docx 258 KB. **Aktornya `Staff Klinik` dan `Pengunjung`** — bukan Developer/API Client, penanda paling menentukan bahwa produk membaca aplikasi dan bukan mengarang dari nama file. `app_description` benar ("sistem manajemen klinik hewan"), dan kelima fiturnya memetakan satu-satu ke controller nyata (Owner/Pet/Visit/Vet/Welcome). **Diagramnya diaudit dengan menggeledah SELURUH Contract A, bukan `dependencies` saja** (pelajaran dari salah-tuduh "Neon"): `Backend --> Database` berjejak kuat — `JpaRepository`, `Entity`×18, `Table`, `Column`, `GeneratedValue`, `ManyToOne`, `Transactional`; "pergantian bahasa tampilan" yang sempat dicurigai karangan ternyata ada (`LocaleResolver`, `LocaleChangeInterceptor`, `SessionLocaleResolver`); "Server-rendered Views" juga (`ModelAndView`, `WebMvcConfigurer`). **Ini kontras tajam dengan fastapi** — satu-satunya kasus karangan yang terkonfirmasi — yang menggambar Database dengan NOL dependency database; petclinic jadi bukti ketiga (sesudah realworld & esteler) bahwa LLM **memang** menurunkan diagram dari kode. Keempat endpoint hasil penyambungan prefix class sampai utuh ke dokumen (`/owners/{ownerId}/pets/{petId}/visits/new`), termasuk `/vets` yang sempat jadi `/`. **Menguatkan pelajaran lama untuk ketiga kalinya: label `type` bukan bottleneck** — coverage cuma 33% dan `Owner.java`/`Pet.java`/`Vet.java` semuanya berlabel `other` (heuristik cuma baca nama file), tapi LLM tetap mengenalinya sebagai model karena membaca `@Entity` dari `dependencies`.
- **2026-07-16** — **Contract A berhenti dikirim ber-indentasi: hemat 30% token, dan `saleor` jadi MUAT.** `llm_service` mengirim `json.dumps(ctx, indent=2)`; spasi indentasi tidak menambah informasi apa pun untuk LLM — **dia cuma dibayar**. Diukur pada saleor: `indent=2` = **1.224.476 token (122% context window, DITOLAK)** vs compact rapat `separators=(",",":")` = **854.181 token (85%, MUAT)**. Jadi aplikasi e-commerce Django nyata bisa dilayani hanya dengan berhenti membayari spasi — tanpa menyentuh arsitektur. **Sekaligus ketahuan alat ukur kita sendiri berbohong**: `scripts/validation/` melaporkan `contract_a_kb` dari `json.dumps()` compact sementara yang dikirim `indent=2`, jadi angkanya **meleset 21-25%** (medusa dilaporkan 2.831 KB; yang dikirim 1,58 juta token) — dan tiap keputusan yang bersandar padanya ikut meleset. Kelas kesalahan yang sama seperti sebab-tertelan-gejala, sekarang di instrumennya. Diperbaiki dengan **satu sumber kebenaran**: `format_contract_a()` diekspor dari `llm_service`, dan harness memakainya — dua tempat men-serialisasi hal yang sama adalah dua tempat yang bisa berbeda diam-diam. **Kualitas diverifikasi lewat A/B di repo nyata** (esteler, ~$0,24, repo & prompt sama, cuma format JSON beda): 10→11 fitur, 9→8 use case, aktor tetap Admin/Customer, diagram tetap berjejak (Groq/Cloudinary/PostgreSQL) — semua dalam rentang variansi antar-run yang normal, tidak ada yang turun. Catatan jujur: A/B ini **tidak bisa memisahkan** efek compact dari non-determinisme LLM; untuk itu perlu beberapa run per varian.
- **2026-07-16** — **Monorepo: `workspaces` dibaca sebagai deklarasi lokasi paket; `www/` medusa (22%) berhenti mencemari dokumen.** `manifest.py` dulu MENDETEKSI monorepo lalu **sengaja menyerah** (`if data.get("workspaces"): return set()` — komentarnya: *"terlalu berisiko ditebak di sini"*). Itu keputusan sadar, bukan kelalaian — tapi petunjuknya ternyata ada di **field yang sama yang dipakai untuk menyerah**: `workspaces` mendeklarasikan di mana paket-paketnya berada. Filosofi yang sama dengan seluruh modul: tanya repo-nya, jangan menebak. **Terukur pada medusa: 9.459 → 7.355 file, dan `www/` (situs dokumentasi) 2.098 → 0.** Nol regresi: fastapi 49→49, flask 25→25, express 8→8. Dua bentuk `workspaces` ditangani, **keduanya dari repo sungguhan**: `list` (cal.com) dan `dict {"packages": [...]}` (medusa, twenty — bentuk yarn). Wildcard sengaja **tidak** dicocokkan persis — begitu ketemu `*`, mundur ke folder induk. Ini bukan malas: test menangkap bahwa menafsirkan `packages/foo*` harfiah jadi `packages/foo/` (folder yang tidak ada) membuat `packages/foo-a/` **ikut terbuang padahal dia workspace yang dideklarasikan** — over-filtering, arah yang paling ditakutkan modul ini. `integration-tests/**/*` yang medusa deklarasikan tetap disimpan: menyaring folder test itu urusan `filters.py`, bukan modul ini.
- **2026-07-16** — **`POST /documents/generate` jadi ASYNC + database job (SQLite). Penghalang produksi terbesar selesai.** Versi lama menahan SELURUH pipeline di satu request HTTP — terukur **191 detik** pada repo nyata — sementara proxy/load balancer memutus di 30-60 detik (Heroku 30, nginx & AWS ALB 60, Vercel 10-60). Di localhost tidak ada satu pun batas itu, jadi bug-nya tidak terlihat sampai di-deploy; dan ketika koneksinya diputus, **server tetap lanjut bekerja dan tetap membayar LLM** untuk dokumen yang tidak pernah sampai ke siapa pun. Sekarang: POST balik **202 + job_id dalam ~50ms** (diukur di server hidup, bukan test), kerja jalan lewat `BackgroundTasks`, klien polling `GET /documents/jobs/{id}` lalu unduh. **Database ikut karena async menuntutnya**: begitu POST balik duluan, harus ada tempat menaruh status dan hasil — sebelumnya tidak ada apa pun, `FileResponse` mengirim file dari temp lalu jejaknya hilang. `sqlite3` stdlib, **nol dependency baru, nol layanan tambahan** — skemanya sengaja rata (bukan ORM) supaya pindah ke Postgres cuma soal mengganti isi `job_store.py`. **Yang paling penting dijaga: pemetaan exception → kode HTTP tidak hilang.** 413 (repo kebesaran) dan 500 (dokumen terpotong) itu kegagalan permanen, dan pengguna harus tetap bisa membedakannya dari 502 "coba lagi" — kalau semua kegagalan job dilaporkan sama, kita balik ke penyamaran yang sudah tiga kali diperbaiki. Karena itu job menyimpan `error_status`. **Dan penyamaran itu nyaris terjadi lagi di sini**: `except RuntimeError` (untuk "Pandoc tidak ada") sempat ditaruh membungkus seluruh pipeline, sehingga `RuntimeError` dari LLM dilaporkan sebagai "Pandoc tidak tersedia" — ditangkap test, diperbaiki dengan `PandocUnavailableError` yang ditangkap di sekeliling `generate_docx()` saja. Batas yang diketahui & diterima ada di Keterbatasan (job hilang kalau proses mati, serverless). **Multi-worker sempat dicatat sebagai batas dan itu SALAH** — diukur kemudian: `--workers 3`, 50 GET lintas worker 0 kali 404, 30 POST bersamaan 30 sukses. Klaim itu ditulis dengan mencocokkan pola kegagalan klasik BackgroundTasks (state di `dict` memori), padahal state di sini ada di SQLite — file di disk yang dibaca semua proses. Itu justru yang membuatnya jalan.
- **2026-07-16** — **DOKUMEN PERTAMA UNTUK APLIKASI BISNIS NYATA BERBAHASA PYTHON — sesuatu yang belum pernah terjadi sepanjang umur project ini.** Dibuat untuk `esteler-app` (Flask, aplikasi pemesanan Es Teler milik anggota tim, ~$0,24): 10 fitur, 9 use case, 8 test case, 9 activity diagram, docx 408 KB. **Kualitasnya kuat dan terverifikasi bukan karangan**: `app_description` menyebut pre-order/walk-in/pembayaran WhatsApp/chatbot — semuanya benar; sepuluh fiturnya memetakan satu-satu ke `services/*` dan endpoint yang nyata (Manajemen Menu, Dashboard Analitik, Keranjang, Rating, Chatbot, Rekomendasi); aktornya **Admin & Customer**, bukan Developer/API Client. **Diagramnya sepenuhnya berdasar bukti** — termasuk `Database PostgreSQL (Neon)`, yang sempat saya tuduh karangan sampai pemilik repo mengoreksi: "Neon" memang ada di Contract A, di **docstring** `config.py::_normalize_db_url()`, dan parser memang mengekstrak docstring. Pelajarannya dicatat di Keterbatasan: `dependencies` bukan satu-satunya bukti di Contract A. Yang membuka jalan: **deteksi endpoint Flask** (`@bp.route(...)`) di `parser_service.py` — `route` bukan anggota `HTTP_METHODS`, jadi Flask menghasilkan **nol** endpoint selama ini. Perbaikannya berantai jauh melampaui endpoint: esteler **0 → 38 endpoint**, coverage **41% → 55%**, dan `routes/` otomatis dikenali `controller` tanpa menyentuh heuristik `type` sama sekali (sinyal `has_endpoints` yang sudah ada langsung bekerja begitu endpoint terlihat). Satu decorator bisa jadi banyak endpoint (`methods=["GET","POST"]` = 2) dan `methods=` yang dihilangkan berarti GET — dua-duanya diambil dari kode nyata, bukan diasumsikan. `parser_service.py` yang selama ini nol test sekarang punya 7 test untuk bagian ini.
- **2026-07-16** — **`max_tokens` naik 16.000 → 32.000 dan pindah ke `.stream()`; pemotongan dokumen sekarang mengaku.** Ditemukan saat generation esteler pertama gagal dengan `ValidationError: Invalid JSON: EOF while parsing a string` — terbaca seperti LLM mengeluarkan sampah, padahal dokumennya **terpotong** karena menabrak `max_tokens`. Ini **kelas kesalahan yang sama untuk ketiga kalinya** (sesudah 502→414 mermaid dan 502→context window): sebab asli tertelan gejala. Sebabnya berlapis: **`claude-sonnet-5` menjalankan adaptive thinking secara DEFAULT** kalau field `thinking` tidak diisi (beda dari Sonnet 4.6 yang default-nya mati), dan `max_tokens` itu batas **total** thinking+teks — thinking memakan ~11K dari 16K, menyisakan ~5K untuk JSON. Ditambah deteksi endpoint Flask bikin dokumennya jauh lebih kaya. Thinking sengaja **tidak** dimatikan (tugas ini justru sintesis, persis yang diuntungkan thinking); jatahnya yang dinaikkan. `.stream()` menggantikan `.parse()` sesuai saran yang sudah tercatat di CLAUDE.md sendiri sejak timeout dinaikkan jadi 1500 detik. `DocumentTruncatedError` memeriksa `stop_reason == "max_tokens"` lewat `current_message_snapshot` → HTTP **500** dengan pesan yang menyebut tempat memperbaikinya, bukan 502 "coba lagi".
- **2026-07-15** — **Dua bug yang menolak/menyesatkan aplikasi bisnis nyata, ditemukan oleh kasus validasi baru (saleor + medusa).** (1) **Tidak ada guard terhadap Contract A yang melebihi context window.** Terukur: Contract A saleor = **1.226.875 token vs batas 1M** — panggilan berbayar tetap dikirim hanya untuk ditolak API, lalu `except Exception` di `routes_document.py` meratakannya jadi 502 *"Coba lagi beberapa saat"* — saran yang tidak akan pernah menolong karena kegagalannya permanen. **Kelas kesalahan yang sama persis dengan 502 yang dulu menelan 414 mermaid.ink**: sebab spesifik disamarkan jadi ajakan mengulang. Sekarang `_guard_context_window` di `llm_service.py` memeriksa lewat `count_tokens` (gratis) sebelum membayar, dan gagal dengan `ContextWindowExceededError` yang menyebut angka aslinya → HTTP **413**, bukan 502. Batas context window **ditanyakan ke Models API**, tidak di-hardcode (daftar hardcoded pasti basi, dan yang basi diam-diam mengembalikan bug ini); gagal-membuka kalau API tak terjangkau. (2) **Guard anti-bomb mencacah seluruh isi arsip sebelum menyaring relevansi**, jadi monorepo ditolak karena banyak dokumentasi/gambar — bukan karena banyak kode. Terukur di medusa: **22.966 member file, cuma 9.459 relevan (41%), dan cuma 23 MB yang diekstrak** — jauh di bawah batas 200 MB, tapi ditolak di pintu. Filter dinaikkan ke atas pencacah di **kedua** provider (tarball & ZIP punya bug yang sama), dan `MAX_TOTAL_FILES` 5.000 → 20.000 (angka lama warisan dari saat pencacahnya menghitung semua; yang mengikat sebenarnya batas byte, jadi batas memori tidak berubah). Dibuktikan ke repo nyata, bukan cuma test: medusa yang tadinya ditolak sekarang terparse (9.459 file), dan saleor gagal dengan pesan yang benar **tanpa membayar sepeser pun**. 100 test hijau (91 + 9). Biaya sesi: **$0**.
- **2026-07-15** — **Penanda `(diisi manual)` sekarang ditanyakan lewat form: dokumen keluar utuh, bukan berlubang.** Jumlah sebenarnya **28** (18 SDD + 10 UAT), bukan 16 seperti tercatat sebelumnya — hitungan lama cuma mencacah SDD, itu pun melewatkan blok terbesarnya (tabel Demografi, 7 baris), dan UAT tidak dihitung sama sekali. Dari 28: **25 ditutup lewat form** (`DocumentMetadata` di `schemas_document.py` → `document_metadata` di `GenerateDocumentRequest` → `meta.*` di template), **3 sengaja dibiarkan** dan kalimatnya diubah supaya terbaca sebagai desain, bukan lubang kelupaan: dua blok tanda tangan (tanda tangan bukan data yang diketik di form) dan Sertifikasi Keberhasilan UAT (berisi tanggal penyelesaian + hasil Lolos/Gagal — belum ada wujudnya saat generate; menanyakannya di form berarti mengundang orang mensertifikasi tes yang belum jalan). **Semua field opsional**, dan yang kosong jatuh balik ke penanda lama — jadi perilaku sebelumnya adalah *lantai*, bukan langit: form boleh dilewati total, dan repo tanpa konteks enterprise (mis. proyek open-source yang tidak punya nomor RFC) tetap bisa digenerate. Dibuktikan lewat mutation check: hapus satu field mana pun dari 17 field SDD → penandanya balik tepat 1×, lengkap → 0×; jadi pemetaan field↔lubang benar-benar 1:1. 90 test hijau (82 + 8 baru).
- **2026-07-14** — Peran 1 (ingestion multi-source + parser Tree-sitter) selesai & merge ke `develop` (PR #1). Peran 2 versi pertama merge (PR #2), pakai LangChain + Google Gemini (`gemini-2.5-flash`).
- **2026-07-14/15** — Peran 3 (compiler service, template Jinja2, endpoint orkestrator, frontend React+Vite) merge ke `develop` (PR #4, #5) — pipeline penuh `ingest -> parse -> LLM -> template -> docx` tersambung lewat `POST /documents/generate` untuk pertama kali.
- **2026-07-15** — **Cross-repo mapping FE↔BE akhirnya terbukti jalan — pembeda utama produk, dan sampai hari itu belum pernah diuji sama sekali.** Tahap 2 dijalankan ke `realworld-fullstack` (~$0,15, Sonnet 5), aplikasi bisnis nyata (kloningan Medium) dengan bahasa yang didukung parser. `component_integration` menghasilkan pemetaan konkret: `Login.js -> POST /users/login`, `Register.js -> POST /users`, `Home/index.js -> GET /articles`, `Editor.js -> POST /articles`, dst — komponen FE dipetakan ke endpoint BE yang benar, lintas dua repo terpisah. Ini juga menjawab pertanyaan "apakah produk ini diam-diam mengasumsikan setiap repo adalah aplikasi bisnis?": **ya, dan itu tidak masalah** — begitu repo-nya memang aplikasi bisnis, formatnya pas. Aktornya jadi `Guest` dan `Registered User` (bukan `Developer`/`API Client` seperti pada framework), fiturnya bisnis nyata (Autentikasi, Manajemen Artikel, Komentar, Favorit, Follow, Tag), dan namanya dikenali dari kode (`Conduit`). **Pelajaran metodologis: kejanggalan pada dokumen fastapi/flask kemarin itu salah PILIHAN KASUS UJI, bukan cacat produk.** Framework memang tidak punya aktor bisnis. Semua penilaian kualitas sebelumnya diambil dari framework/library — bukan dari target produk yang sebenarnya.
- **2026-07-15** — **Diverifikasi lewat API sungguhan bahwa rantai perbaikan hari ini benar-benar memperbaiki produknya, bukan cuma angkanya** (~$0,41, Sonnet 5). (1) **Caching aktif**: panggilan SDD lalu UAT pada Contract A yang sama → `cache_write=7919 / cache_read=0`, lalu `cache_write=0 / cache_read=7919`; cuma 30 token dibayar penuh. Pada fastapi, `cache_write=63679`. (2) **`target_doc_type` tetap membedakan** setelah urutan prompt dibalik — SDD 5 test case vs UAT 9, sisanya sama. (3) **SDD fastapi sekarang jujur**: dulu "Sistem ini adalah kumpulan aplikasi backend... mengelola data, unggah berkas" dengan fitur "Manajemen Item" dan "Manajemen Hero" (nama tabel dari tutorial SQLModel); sekarang **"FastAPI adalah framework backend berbasis Python"** dengan fitur Routing & Endpoint, Injeksi Dependensi, Validasi Data, Serialisasi, Keamanan, Dokumentasi Otomatis (OpenAPI/Swagger), dan WebSocket — semuanya fitur FastAPI yang sebenarnya, dan tetap ditulis untuk pembaca non-teknis. Contract B-nya disimpan di `scripts/validation/out/` (di-gitignore) supaya bisa dibaca ulang tanpa membayar lagi.
- **2026-07-15** — **Urutan prompt dibalik supaya Contract A bisa di-cache.** Prompt caching itu **cocok-dari-depan** (prefix match): satu byte berbeda membatalkan semua sesudahnya. Versi lama menaruh `target_doc_type` di depan Contract A, jadi ketika repo yang sama diminta SDD lalu UAT, Contract A yang puluhan ribu token itu dibayar **penuh dua kali** padahal isinya identik. Sekarang Contract A duluan (dengan `cache_control` di ujungnya), `target_doc_type` belakangan — panggilan kedua cuma bayar ~10% untuk bagian itu. Determinisme Contract A sudah dibuktikan (parse flask dua kali → sha256 identik; `dependencies` memang `list` yang di-`append`, bukan `set`), jadi cache-nya benar-benar bisa nyantol. `LLMService` sekarang juga mencatat `cache_read`/`cache_write` ke log — tanpa itu, cache yang diam-diam tidak pernah aktif mustahil ketahuan; gejalanya cuma tagihan lebih mahal dari perkiraan. Catatan: minimum cacheable ~2.048 token (Sonnet 5) / 4.096 (Opus 4.8) — prompt kecil tidak akan pernah ter-cache, dan `SYSTEM_PROMPT` sendiri (~2.358 token) terlalu kecil untuk di-cache sendirian di Opus.
- **2026-07-15** — **Deteksi produk dari manifest (`app/ingestion/manifest.py`) menggantikan tebak-tebakan blocklist.** Alih-alih menebak folder mana yang *bukan* produk (permainan yang tidak bisa dimenangkan — `test/` tertangkap, `examples/` lolos, lalu `docs_src/`, lalu `website/`...), sekarang **repo-nya sendiri yang ditanya**: `pyproject.toml` dan `package.json` mendeklarasikan apa yang mereka kirim. Diterapkan di `ingestion_service.narrow_to_product()` — sesudah provider, bukan di dalamnya, karena butuh melihat seluruh repo, dan supaya jalur GitHub & ZIP tunduk pada aturan yang sama. **Hasil pada repo nyata: fastapi 530 -> 49 file**, dan endpoint **432 -> 0** — yang **benar**, karena fastapi si framework *mendefinisikan* `@app.route`, tidak memakainya; 432 tadi semuanya milik tutorial `docs_src/`. Repo yang memang sudah bersih tidak berubah sama sekali (flask 25->25, express 8->8, requests 21->21), jadi deteksinya tidak asal membuang. Biaya input fastapi turun 48% (191.880 -> 60.907 token; ~$0,54 -> ~$0,28 dengan Sonnet 5). Format manifest sangat beragam dan **semua varian di bawah ini diambil dari repo sungguhan, bukan diasumsikan**: fastapi cuma `name = "fastapi"` (package di root); flask `name = "Flask"` huruf besar tapi modulnya `flask` di `src/flask/`; requests `where = ["src"]`; express `files: ["index.js", "lib/"]` (npm menyebut langsung isi paketnya — sinyal terbaik yang ada). **Desainnya sengaja gagal-membuka**: manifest tidak ada/rusak/tidak cocok dengan file mana pun -> simpan SEMUA file. Filter rakus membuang kode produksi diam-diam, dan itu kegagalan yang jauh lebih sulit dilihat daripada kebanyakan noise (lihat regresi `samples` di entri berikutnya).
- **2026-07-15** — **Tahap 2 (berbayar) dijalankan pertama kali: flask + fastapi, SDD.** Tujuannya menjawab satu pertanyaan sebelum menghabiskan waktu memperbaiki heuristik `type`: seberapa parah dampak label `"other"` ke dokumen jadinya? Jawabannya: **tidak parah — label bukan bottleneck**. flask (coverage 0%) menghasilkan 11 fitur yang akurat, malah lebih banyak dari fastapi (coverage 63%) yang cuma 10 dan isinya salah. Ini menghemat kerja berhari-hari yang nyaris dilakukan ke masalah yang ternyata tidak ada. Sekaligus mengungkap masalah yang jauh lebih penting — kontaminasi input `docs_src/`, lihat Keterbatasan. Pipeline penuh terbukti jalan ke repo nyata: 8 diagram Mermaid ter-embed di SDD flask (bukti encoding pako jalan end-to-end), generation ~103-109 detik per dokumen. Biaya: 2 panggilan Claude. **Pelajaran metodologis: ukur dulu sebelum memperbaiki.**
- **2026-07-15** — **Kerangka validasi dibuat (`scripts/validation/`) dan langsung berbuah.** Dua tahap dipisah berdasarkan biaya: Tahap 1 (`ingest -> parse`, gratis, tanpa LLM) dan Tahap 2 (`--with-llm`, berbayar, opt-in). Menjawab pertanyaan yang tidak bisa dijawab `pytest`: bukan "apakah kode sesuai rancangan?" tapi "apakah rancangan bertahan di repo asing?". Tiga temuan dari 7 repo publik: (1) ingestion 1-request-per-file (sudah diperbaiki, lihat entri berikutnya); (2) kode test & contoh ikut ter-ingest — pada express, **1169 dari 1265 endpoint ternyata berasal dari `test/`**, Contract A menyusut 99.5 KB -> 4.3 KB setelah disaring (sudah diperbaiki, lihat `filters.py`); (3) heuristik `type` gagal pada penamaan idiomatik Python (**belum** diperbaiki, lihat Keterbatasan). Catatan: metrik `coverage` di harness masih cacat — `flask 0%` (kode terbaca tapi salah label) dan `gin-go 0%` (kode sama sekali tidak terbaca) tampil identik padahal butuh perbaikan yang berbeda.
- **2026-07-15** — **Ingestion GitHub pindah dari per-file ke tarball sekali unduh.** `github_provider.py` dulu memanggil `get_git_blob()` **satu request per file**, jadi repo 200 file = 200 request berurutan. Akibatnya batas anonim GitHub (60 request/jam) habis sebelum satu repo pun selesai — mencoba `express` (repo kecil) menghabiskan seluruh jatah lalu menggantung >7 menit tanpa sebab yang kelihatan. Sekarang repo diunduh sebagai tarball dalam **satu** request lalu dibongkar di memori, jadi biaya API turun dari ~N request (N = jumlah file) jadi ~2 per repo, konstan berapa pun ukuran reponya. Guard anti archive-bomb dipindah ke `filters.py` supaya jalur ZIP dan tarball tunduk pada batas yang sama. Ditemukan oleh kerangka validasi di `scripts/validation/` pada percobaan perdananya.
- **2026-07-15** — **Bug render diagram untuk repo besar selesai.** Gejalanya dulu tercatat sebagai "502 Gagal merender diagram" dan diduga soal panjang URL. Dugaan itu benar soal akar masalahnya, tapi 502-nya menyesatkan: itu **pesan error aplikasi sendiri**. `_render_docx_or_502` meratakan SEMUA `requests.RequestException` jadi 502 "layanan tidak merespons" — padahal `raise_for_status()` melempar `HTTPError` (subclass-nya) saat mermaid.ink membalas **`414 URI Too Long`**. Jadi mermaid.ink sebetulnya menjawab dengan jelas, cuma jawabannya ditelan error handler kita. Diperbaiki tiga lapis: (1) encoding `pako:` (zlib) menggantikan base64 polos — kompresi ~7x, diagram 120-node turun dari 9.376 jadi 1.273 karakter URL; (2) `DiagramRenderError` membawa sebab asli (status HTTP + artinya) alih-alih menyamarkannya; (3) code fence ```` ```mermaid ```` dari LLM dibuang otomatis (dulu bikin `400`). Semua angka di atas diukur langsung ke mermaid.ink, bukan diperkirakan.
- **2026-07-15** — Migrasi provider LLM: **Google Gemini (LangChain) -> Anthropic Claude (`claude-opus-4-8`, SDK resmi langsung)**. Sekaligus memperbaiki bug `target_doc_type` yang sebelumnya tidak berpengaruh ke output (selalu bergaya SDD apa pun tipe dokumennya), dan memperkaya Contract B dengan `feature_requirements[]` dan `use_cases[]` (menutup dua bagian template SDD yang sebelumnya kosong/`(diisi manual)`: "Application Features Requirement" dan tabel Use Case per aktor). Perubahan sudah diverifikasi lewat pemanggilan API sungguhan (bukan cuma mock test), termasuk lewat `POST /documents/generate` terhadap repo asli.
